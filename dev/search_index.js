var documenterSearchIndex = {"docs":
[{"category":"section","location":"3-mixed-integer-optimization/#Example:-Mixed-integer-optimization-(MILP)","page":"Example: Mixed integer optimization (MILP)","text":"This example demonstrates the extension of ConstraintTree bounds structures to accommodate new kinds of problems. In particular, we create a new kind of Bound that is restricting the value to be a full integer, and then solve a geometric problem with that.","title":"Example: Mixed integer optimization (MILP)"},{"category":"section","location":"3-mixed-integer-optimization/#Creating-a-custom-bound","page":"Example: Mixed integer optimization (MILP)","text":"All bounds contained in constraints are subtypes of the abstract ConstraintTrees.Bound. These include ConstraintTrees.EqualTo and ConstraintTrees.Between, but the types can be extended as necessary, given the final rewriting of the constraint system to JuMP can handle the new bounds.\n\nLet's make a small \"marker\" bound for something that needs to be integer-ish, between 2 integers:\n\nimport ConstraintTrees as C\n\nmutable struct IntegerFromTo <: C.Bound\n    from::Int\n    to::Int\nend\n\nWe can now write e.g. a bound on the number on a thrown six-sided die as follows:\n\nIntegerFromTo(1, 6)\n\n...and include this bound in constraints and variables:\n\ndice_system = C.variables(keys = [:first_dice, :second_dice], bounds = IntegerFromTo(1, 6))\n\nNow the main thing that is left is to be able to translate this bound to JuMP for solving. We can slightly generalize our constraint-translation system from the previous examples for this purpose, by separating out the functions that create the constraints:\n\nimport JuMP\n\nfunction jump_constraint(m, x, v::C.Value, b::C.EqualTo)\n    JuMP.@constraint(m, C.substitute(v, x) == b.equal_to)\nend\n\nfunction jump_constraint(m, x, v::C.Value, b::C.Between)\n    isinf(b.lower) || JuMP.@constraint(m, C.substitute(v, x) >= b.lower)\n    isinf(b.upper) || JuMP.@constraint(m, C.substitute(v, x) <= b.upper)\nend\n\nJuMP does not support direct integrality constraints, so we need to make a small digression with a slack variable:\n\nfunction jump_constraint(m, x, v::C.Value, b::IntegerFromTo)\n    var = JuMP.@variable(m, integer = true)\n    JuMP.@constraint(m, var >= b.from)\n    JuMP.@constraint(m, var <= b.to)\n    JuMP.@constraint(m, C.substitute(v, x) == var)\nend\n\nfunction milp_optimized_vars(cs::C.ConstraintTree, objective::C.Value, optimizer)\n    model = JuMP.Model(optimizer)\n    JuMP.@variable(model, x[1:C.variable_count(cs)])\n    JuMP.@objective(model, JuMP.MAX_SENSE, C.substitute(objective, x))\n    C.traverse(cs) do c\n        isnothing(c.bound) || jump_constraint(model, x, c.value, c.bound)\n    end\n    JuMP.set_silent(model)\n    JuMP.optimize!(model)\n    JuMP.value.(model[:x])\nend\n\nLet's try to solve a tiny system with the dice first. What's the best value we can throw if the dice are thrown at least 1.5 points apart?\n\ndice_system *=\n    :points_distance^C.Constraint(\n        dice_system.first_dice.value - dice_system.second_dice.value,\n        C.Between(1.5, Inf),\n    )\n\nFor solving, we use GLPK (it has MILP capabilities).\n\nimport GLPK\ndices_thrown = C.substitute_values(\n    dice_system,\n    milp_optimized_vars(\n        dice_system,\n        dice_system.first_dice.value + dice_system.second_dice.value,\n        GLPK.Optimizer,\n    ),\n)","title":"Creating a custom bound"},{"category":"section","location":"3-mixed-integer-optimization/#A-note-on-pretty-printing-of-custom-extensions","page":"Example: Mixed integer optimization (MILP)","text":"By default, pretty-printing via pretty attempts to fall back to Base.show for any value which has no explicit overload of pretty. In particular, the bounds in our MILP system are formatted as follows:\n\nC.pretty(dice_system)\n\nTo provide a prettier rendering, it is sufficient to provide a matching overload of pretty:\n\nfunction C.pretty(io::IO, x::IntegerFromTo; style_args...)\n    print(io, \" ∈ {$(x.from) … $(x.to)}\")\nend\n\nC.pretty(dice_system)","title":"A note on pretty-printing of custom extensions"},{"category":"section","location":"3-mixed-integer-optimization/#A-more-realistic-example-with-geometry","page":"Example: Mixed integer optimization (MILP)","text":"Let's find the size of the smallest right-angled triangle with integer side sizes (aka a Pythagorean triple).\n\nvars = C.variables(keys = [:a, :b, :c], bounds = IntegerFromTo(1, 100))\n\nFor simpliclty, we make a shortcut for \"values\" in all variables:\n\nv = C.map(C.value, vars, C.Value)\n\nWith that shortcut, the constraint tree constructs quite easily:\n\ntriangle_system =\n    :sides^vars *\n    :circumference^C.Constraint(sum(values(v))) *\n    :a_less_than_b^C.Constraint(v.b - v.a, (0, Inf)) *\n    :b_less_than_c^C.Constraint(v.c - v.b, (0, Inf)) *\n    :right_angled^C.Constraint(C.squared(v.a) + C.squared(v.b) - C.squared(v.c), 0.0)\n\nC.pretty(triangle_system, format_variable = i -> [\"\", \"A\", \"B\", \"C\"][i+1])\n\nWe will need a solver that supports both quadratic and integer optimization:\n\nimport SCIP\ntriangle_sides = C.substitute_values(\n    triangle_system,\n    milp_optimized_vars(\n        triangle_system,\n        -triangle_system.circumference.value,\n        SCIP.Optimizer,\n    ),\n).sides\n\n\n\nThis page was generated using Literate.jl.","title":"A more realistic example with geometry"},{"category":"section","location":"4-functional-tree-processing/#Example:-Processing-the-trees-functionally","page":"Example: Processing the trees functionally","text":"The main goal of ConstraintTrees.jl is to make the constraint-manipulating code orderly and elegant, and preferably short. To improve the manipulation of large constraint trees, the package also provides a small functional-programming-inspired framework that allows one to easily transform, summarize and combine all kinds of trees without writing repetitive code.\n\nYou might already seen the zip function in the metabolic modeling example. There are more functions that behave like zip, so let's have a little summary here:\n\nmap applies a function to all elements (including the nested ones) of a tree\nmapreduce transforms all elements of a tree using a given function (first parameter) and then combines the result using the second function (a binary operator); reduce is a shortcut where the map function is an identity\nzip combines elements common to both trees using a given zipping function\nmerge combines all elements in both trees (including the ones present in only one tree) using a given merging function\nvariables_for allocates a variable for each constraint in the tree and allows the user to specify bounds\n\nAdditionally, all these have their \"indexed\" variant which allows the user to know the path where the tree elements are being merged. The path is passed to the handling function as a tuple of symbols. The variants are prefixed with i:\n\nimap\nimapreduce (here the path refers to the common directory of the reduced elements) together with the shortcut ireduce\nizip\nimerge\nvariables_ifor\n\ndanger: Naming conflicts with Julia base\nNames of some of the higher-order function conflict with Julia base package and are not compatible. We recommend using them with named imports, such as by import ConstraintTrees as C and then C.zip and C.merge.\n\nFor demonstration, let's make a very simple constrained system.\n\nimport ConstraintTrees as C\n\nconstraints = :point^C.variables(keys = [:x, :y], bounds = C.Between(0, 1))\n\nC.pretty(constraints)","title":"Example: Processing the trees functionally"},{"category":"section","location":"4-functional-tree-processing/#Transforming-trees-with-map","page":"Example: Processing the trees functionally","text":"Let's make a tree where the bounds are 2 times bigger and negated:\n\nx = C.map(constraints) do x\n    C.Constraint(x.value, -2 * x.bound)\nend\n\nC.pretty(x)\n\nWith imap, we can detect that we are working on a specific constraint and do something entirely different:\n\nx = C.imap(constraints) do path, x\n    if path == (:point, :x)\n        C.Constraint(x.value, 100 * x.bound)\n    else\n        x\n    end\nend\n\nC.pretty(x)","title":"Transforming trees with map"},{"category":"section","location":"4-functional-tree-processing/#Summarizing-the-trees-with-mapreduce-and-reduce","page":"Example: Processing the trees functionally","text":"How many constraints are there in the tree?\n\nx = C.mapreduce(init = 0, _ -> 1, +, constraints)\n\nWhat if we want to sum all constraints' values?\n\nx = C.reduce(constraints, init = C.Constraint(zero(C.LinearValue))) do x, y\n    C.Constraint(value = x.value + y.value)\nend\n\nWhat if we want to reduce the point specially?\n\nx = C.ireduce(constraints, init = C.Constraint(zero(C.LinearValue))) do path, x, y\n    if path == (:point,)\n        println(\"reducing in point/ subtree: $(x.value) + $(y.value)\")\n    end\n    C.Constraint(value = x.value + y.value)\nend;\nnothing #hide\n\nx","title":"Summarizing the trees with mapreduce and reduce"},{"category":"section","location":"4-functional-tree-processing/#Comparing-trees-with-zip","page":"Example: Processing the trees functionally","text":"Assume we have two solutions of the constraint system above, as follows:\n\ns1 = C.substitute_values(constraints, [0.9, 0.8])\ns2 = C.substitute_values(constraints, [0.99, 0.78])\n\nLet's compute the squared distance between individual items:\n\nx = C.zip(s1, s2, Float64) do x, y\n    (x - y)^2\nend\n\nC.pretty(x)\n\nWhat if we want to put extra weight on distances between specific variables?\n\nx = C.izip(s1, s2, Float64) do path, x, y\n    if path == (:point, :x)\n        10\n    else\n        1\n    end * (x - y)^2\nend\n\nC.pretty(x)","title":"Comparing trees with zip"},{"category":"section","location":"4-functional-tree-processing/#Combining-trees-with-merge","page":"Example: Processing the trees functionally","text":"Zipping trees together always produces a tree that only contains the intersection of keys from both original trees. That is not very useful if one wants to e.g. add new elements from extended trees. merge-style functions implement precisely that.\n\nThe \"zipping\" function in merge takes 2 arguments; any of these may be missing in case one of the trees does not contain the elements. Also, a key may be omitted by returning missing from the function.\n\nLet's make some very heterogeneous trees and try to combine them:\n\nt1 = :x^s1.point * :y^s2.point;\nt2 = :x^s2.point * :z^s1.point;\nt3 = :y^s2.point;\nnothing #hide\n\nAs a nice combination function, we can try to compute an average on all positions from the first 2 trees:\n\nt = C.merge(t1, t2, Float64) do x, y\n    ismissing(x) && return y\n    ismissing(y) && return x\n    (x + y) / 2\nend\n\nC.pretty(t)\n\nMerge can also take 3 parameters (which is convenient in some situations). We may also want to omit certain output completely:\n\ntz = C.merge(t1, t2, t3, Float64) do x, y, z\n    ismissing(z) && return missing\n    ismissing(x) && return y\n    ismissing(y) && return x\n    (x + y) / 2\nend\n\nC.pretty(tz)\n\nWe also have the indexed variants; for example this allows us to only merge the x elements in points:\n\ntx = C.imerge(t1, t2, Float64) do path, x, y\n    last(path) == :x || return missing\n    ismissing(x) && return y\n    ismissing(y) && return x\n    (x + y) / 2\nend\n\nC.pretty(tx)\n\nFor completeness, we demonstrate a trick with easily coalescing the \"missing\" things to compute the means more easily:\n\nmiss(_::Missing, _, def) = def;\nmiss(x, f, _) = f(x);\nfixmean(a) = miss(a, x -> (x, 1), (0, 0));\n\ntx = C.imerge(t1, t2, t3, Float64) do path, x, y, z\n    last(path) == :x || return missing\n    tmp = fixmean.([x, y, z])\n    sum(first.(tmp)) / sum(last.(tmp))\nend\n\nC.pretty(tx)","title":"Combining trees with merge"},{"category":"section","location":"4-functional-tree-processing/#Allocating-trees-of-variables-using-variables_for","page":"Example: Processing the trees functionally","text":"In many cases it is convenient to make a new model from the old by allocating new variables for whatever \"old\" tree out there. For example, one might wish to allocate a new variable for an approximate value (plus-minus-one) for each of the above tree's values. variables_for allocates one variable for each element of the given tree, and allows you to create bounds for the variables via the given function:\n\nC.pretty(t)\n\nx = C.variables_for(t) do a\n    C.Between(a - 1, a + 1)\nend\n\nC.pretty(x)\n\n(Note that the variable indexes in subtrees are now different from each other!)\n\nAs in all cases with indexes, you may match the tree path to do a special action. For example, to make sure that all y coordinates are exact in the new system:\n\nx = C.variables_ifor(t) do path, a\n    if last(path) == :y\n        C.EqualTo(a)\n    else\n        C.Between(a - 1, a + 1)\n    end\nend\n\nC.pretty(x)","title":"Allocating trees of variables using variables_for"},{"category":"section","location":"4-functional-tree-processing/#Looping-through-the-trees-with-traverse","page":"Example: Processing the trees functionally","text":"Since we are writing our code in an imperative language, it is often quite beneficial to run a function over the trees just for the side effect.\n\nFor this purpose, traverse and itraverse work precisely like map and imap, except no tree is returned and the only \"output\" of the functions are their side effect.\n\nFor example, you can write a less-functional counting of number of constraints in the tree as follows:\n\nconstraint_count = 0\nC.traverse(x) do _\n    global constraint_count += 1\nend\nconstraint_count\n\nThe indexed variant of traverse works as expected; it may be beneficial e.g. for printing the contents of the constraint trees in a \"flat\" form, or potentially working with other path-respecting data structures.\n\nC.itraverse(x) do ix, c\n    path = join(String.(ix), '/')\n    println(\"$path = $c\")\nend;\nnothing #hide\n\nTo prevent uncertainty, both functions always traverse the keys in sorted order.","title":"Looping through the trees with traverse"},{"category":"section","location":"4-functional-tree-processing/#Removing-constraints-with-filter","page":"Example: Processing the trees functionally","text":"In many cases it is beneficial to simplify the constraint system by systematically removing constraints. filter and ifilter run a function on all subtrees and leaves (usually the leaves are Constraints), and only retain these where the function returns true.\n\nFor example, this removes all constraints named y:\n\nfiltered = C.ifilter(x) do ix, c\n    return c isa C.ConstraintTree || last(ix) != :y\nend\n\nC.pretty(filtered)\n\nFunctions filter_leaves and ifilter_leaves act similarly but automatically assume that the directory structure is going to stay intact, freeing the user from having to handle the subdirectories.\n\nThe above example thus simplifies to:\n\nfiltered = C.ifilter_leaves(x) do ix, c\n    last(ix) != :y\nend\n\nC.pretty(filtered)\n\nWe can also remove whole variable ranges:\n\nfiltered = C.filter_leaves(x) do c\n    all(>=(4), c.value.idxs)\nend\n\nC.pretty(filtered)","title":"Removing constraints with filter"},{"category":"section","location":"4-functional-tree-processing/#Pruning-unused-variable-references","page":"Example: Processing the trees functionally","text":"Filtering operations may leave the constraint tree in a slightly sub-optimal state, where there are indexes allocated for variables that are no longer used!\n\nC.variable_count(filtered)\n\nTo investigate, it is possible to calculate a \"reference count\" for each variable:\n\nvariable_ref_counts = zeros(Int, C.variable_count(filtered))\nC.collect_variables!(filtered) do idx\n    if idx > 0\n        variable_ref_counts[idx] += 1\n    end\nend\nvariable_ref_counts\n\nTo fix the issue, it is possible to \"squash\" the variable indexes using prune_variables:\n\npruned = C.prune_variables(filtered)\n\nC.variable_count(pruned)\n\nNote that after the pruning and renumbering, the involved constraint trees are no longer compatible, and should not be combined with *. As an anti-example, one might be interested in pruning the variable values before joining them in to larger constraint tree, e.g. to simplify larger quadratic values:\n\npruned_qv = C.prune_variables(x.y.x.value * x.z.y.value)\n\nThis value now corresponds to a completely different value in the original tree! Compare:\n\n(pruned_qv, x.y.x.value * x.z.y.value)\n\nAs another common source of redundant variable references, some variables may be used with zero weights. This situation is not detected by prune_variables by default, but you can remove the \"zeroed out\" variable references by using drop_zeros, which allows the pruning to work properly.\n\nFor example, the value constructed in the tree below does not really refer to x.x.y anymore, but pruning does not help to get rid of the now-redundant variable:\n\nx.x.y.value = x.x.y.value + x.x.x.value * x.x.x.value - x.x.y.value\n\nC.variable_count(C.prune_variables(x))\n\nAfter the zero-weight variable references are dropped, the pruning behaves as desired:\n\nC.variable_count(C.prune_variables(C.drop_zeros(x)))","title":"Pruning unused variable references"},{"category":"section","location":"4-functional-tree-processing/#Converting-to-and-from-vector-representations","page":"Example: Processing the trees functionally","text":"Often it is useful to look at the constraint system via the \"matrix\" view as common in mathematical optimization. Functions deflate and reinflate provide a sensible way to convert the trees into vectors of constraints and back, giving more possibilities to work with the tree contents.\n\nIn particular, trees that only contain linear values may be represented as matrices. For demonstration, one may convert the following (slightly fixed) tree to a matrix, a vector of lower and upper bounds, and a vector of constraint \"row\" names:\n\nx.x.y.value = 3*x.x.x.value + 2 * x.y.y.value + 0.5 * x.z.x.value\nC.pretty(x)\n\ndeflate serves as a conversion tool to vectors:\n\nC.deflate(x, C.MaybeBound) do c\n    c.bound\nend\n\nTo extract a proper matrix, one has to convert the linear values to actual vectors:\n\nimport SparseArrays: sparse, sparsevec, SparseVector\n\nn_vars = C.variable_count(x)\nvecs = C.deflate(x, SparseVector{Float64}) do c\n    sparsevec(c.value.idxs, c.value.weights, n_vars)\nend;\nsparse(hcat(vecs...)')\n\nFinally, to extract everything at once with proper identifiers, it is useful to use ideflate:\n\nrows = C.ideflate(x, Pair) do i, c\n    join(i, \"/\") => (sparsevec(c.value.idxs, c.value.weights, n_vars), c.bound)\nend;\nnothing #hide\n\nTo convert to the usual form, we use some helper functions:\n\nlb(x::C.EqualTo) = x.equal_to\nub(x::C.EqualTo) = x.equal_to\nlb(x::C.Between) = x.lower\nub(x::C.Between) = x.upper\n\nThis gives good row \"identifiers\":\n\nrow_names = first.(rows)\n\n...as well as lower and upper bound vectors:\n\nrow_bounds = let constraints = last.(last.(rows))\n    (lb.(constraints), ub.(constraints))\nend\n\n...and the \"linear programming\" matrix:\n\nmatrix = sparse(hcat(first.(last.(rows))...)')\n\nThe vector data can be re-inserted into \"same-shaped\" trees. For example, one can make a tree where variable references sum to 1 for each variable:\n\nweighted_matrix = matrix ./ max.(eps(), sum(matrix, dims = 1))\n\nThe weighted matrix is then converted to a vector and re-inserted:\n\nC.reinflate(x, C.LinearValue.(sparse.(eachrow(weighted_matrix)))) |> C.pretty\n\n\n\nThis page was generated using Literate.jl.","title":"Converting to and from vector representations"},{"category":"section","location":"5-jump-integration/#Better-integration-with-JuMP","page":"Better integration with JuMP","text":"The examples in this documentation generally used the simple and straightforward method of converting the trees and values to JuMP system, which depends on algebraic operators working transparently with JuMP values within function substitute.","title":"Better integration with JuMP"},{"category":"section","location":"5-jump-integration/#Substitution-folding-problem","page":"Better integration with JuMP","text":"Despite the simplicity, this approach is sometimes sub-optimal, especially in cases when the result of the substitution is recalculated with added values. For example, in the naive case, JuMP is forced to successively build representations for all intermediate expressions with incomplete variables, until all variables are in place. In turn, this may very easily reach a quadratic computational complexity.\n\nMore generally, any representation of substitution result that \"does not reduce() easily\" will suffer from this problem. A different (often specialized) approach is thus needed.","title":"Substitution folding problem"},{"category":"section","location":"5-jump-integration/#Solution:-Prevent-successive-folding","page":"Better integration with JuMP","text":"For such cases, it is recommended to replace the substitute calls with a custom function that can interpret the required Values itself, and converts them without the overhead of creating temporary values.\n\nimport ConstraintTrees as C\n\nFirst, let's create a lot of variables, and a constraint that will usually trigger this problem (and a JuMP warning) if used with normal substitute:\n\nx = :vars^C.variables(keys = Symbol.(\"x$i\" for i = 1:1000), bounds = C.Between(0, 10))\nx *= :sum^C.Constraint(sum(C.value.(values(x.vars))))\n\nNow, imagine the expressions are represented e.g. by sparse vectors of fixed size (as common in linear-algebraic systems). We can produce the vectors efficiently as follows:\n\nimport SparseArrays: sparsevec\nv = x.sum.value\n\nvalue_in_a_vector = sparsevec(v.idxs, v.weights, 1000)\n\nThis usually requires only a single memory allocation, and runs in time linear with the number of variables in the value. As an obvious downside, you need to implement this functionality for all kinds of Values you encounter.","title":"Solution: Prevent successive folding"},{"category":"section","location":"5-jump-integration/#Solution-for-JuMP","page":"Better integration with JuMP","text":"LinearValues can be translated to JuMP's AffExprs:\n\nusing JuMP, GLPK\n\nfunction substitute_jump(val::C.LinearValue, vars)\n    e = AffExpr() # unfortunately @expression(model, 0) is not type stable and gives an Int\n    for (i, w) in zip(val.idxs, val.weights)\n        if i == 0\n            add_to_expression!(e, w)\n        else\n            add_to_expression!(e, w, vars[i])\n        end\n    end\n    e\nend\n\nmodel = Model(GLPK.Optimizer)\n@variable(model, V[1:1000])\njump_value = substitute_jump(x.sum.value, V)\n\nThis function can be re-used in functions like optimized_vars as shown in other examples in the documentation.\n\nFor QuadraticValues, the same approach extends only with a minor modification:\n\nfunction substitute_jump(val::C.QuadraticValue, vars)\n    e = QuadExpr() # unfortunately @expression(model, 0) is not type stable and gives an Int\n    for ((i, j), w) in zip(val.idxs, val.weights)\n        if i == 0 && j == 0\n            add_to_expression!(e, w)\n        elseif i == 0 # the symmetric case is prohibited\n            add_to_expression!(e, w, vars[j])\n        else\n            add_to_expression!(e, w, vars[i], vars[j])\n        end\n    end\n    e\nend\n\nqvalue = 123 + (x.vars.x1.value + x.vars.x2.value) * (x.vars.x3.value - 321)\njump_qvalue = substitute_jump(qvalue, V)\n\n\n\nThis page was generated using Literate.jl.","title":"Solution for JuMP"},{"category":"section","location":"0-quickstart/#Quick-start","page":"Quick start","text":"The primary purpose of ConstraintTrees.jl is to make the representation of constraint systems neat, and thus make their manipulation easy and high-level. In short, the package abstracts the users from keeping track of variable and constraint indexes in matrix form, and gives a nice data structure that describes the system, while keeping all variable allocation and constraint organization completely implicit.\n\nHere we demonstrate the absolutely basic concepts on the \"field allocation\" problem.","title":"Quick start"},{"category":"section","location":"0-quickstart/#The-problem:-Field-area-allocation","page":"Quick start","text":"Suppose we have 100 square kilometers of field, 500 kilos of fertilizer and 300 kilos of insecticide. We also have a practically infinite supply of wheat and barley seeds. If we decide to sow barley, we can make 550🪙 per square kilometer of harvest; if we decide to go with wheat instead, we can make 350🪙. Unfortunately each square kilometer of wheat requires 6 kilos of fertilizer, and 1 kilo of insecticide, whereas each square kilometer of barley requires 2 kilos of fertilizer and 4 kilos of insecticide, because insects love barley.\n\nHow much of our fields should we allocate to wheat and barley to maximize our profit?","title":"The problem: Field area allocation"},{"category":"section","location":"0-quickstart/#Field-area-allocation-with-ConstraintTrees.jl","page":"Quick start","text":"Let's import the package and start constructing the problem:\n\nimport ConstraintTrees as C\n\nLet's name our system s. We first need a few variables:\n\ns = C.variables(keys = [:wheat, :barley])\n\nWith ConstraintTrees.jl, we can (and want to!) label everything very nicely – the constraint trees are essentially directory structures, so one can prefix everything with symbols to put it into nice directories, e.g. as such:\n\n:area^s\n\nTo be absolutely realistic, we also want to make sure that all areas are non-negative. To demonstrate how to do that nicely from the start, we rather re-do the constraints with an appropriate interval bound:\n\ns = :area^C.variables(keys = [:wheat, :barley], bounds = C.Between(0, Inf))\n\nConstraint trees can be browsed using dot notation, or just like dictionaries:\n\ns.area\n\ns[:area].barley\n\n(For convenience in some cases, string indexes are also supported:)\n\ns[\"area\"][\"barley\"]\n\nThe default rendering of constraint trees is mildly inconvenient because of efficiency constraints and the amount of structural and type information that it must convey. To see the data structure at full scale, we can use pretty:\n\nC.pretty(s)\n\nNow let's start rewriting the problem into the constraint-tree-ish description. First, we only have 100 square kilometers of area:\n\ntotal_area = s.area.wheat.value + s.area.barley.value\n\ntotal_area_constraint = C.Constraint(total_area, (0, 100))\n\nWe can add any kind of constraint into the existing constraint trees by \"merging\" multiple trees with operator *:\n\ns *= :total_area^total_area_constraint\n\nNow let's add constraints for resources. We can create whole ConstraintTree structures like dictionaries in place, as follows:\n\ns *=\n    :resources^C.ConstraintTree(\n        :fertilizer =>\n            C.Constraint(s.area.wheat.value * 6 + s.area.barley.value * 2, (0, 500)),\n        :insecticide =>\n            C.Constraint(s.area.wheat.value * 1 + s.area.barley.value * 4, (0, 300)),\n    )\n\nWe can also represent the expected profit as a constraint (although we do not need to actually put a constraint bound there):\n\ns *= :profit^C.Constraint(s.area.wheat.value * 350 + s.area.barley.value * 550)\n\nFinally, the whole system looks like this:\n\nC.pretty(s)","title":"Field area allocation with ConstraintTrees.jl"},{"category":"section","location":"0-quickstart/#Solving-the-system-with-JuMP","page":"Quick start","text":"We can now take the structure of the constraint tree, translate it to any suitable linear optimizer interface, and have it solved. For popular reasons we choose JuMP with GLPK – the code is left uncommented here as-is; see the other examples for a slightly more detailed explanation:\n\nimport JuMP\nfunction optimized_vars(cs::C.ConstraintTree, objective::C.LinearValue, optimizer)\n    model = JuMP.Model(optimizer)\n    JuMP.@variable(model, x[1:C.variable_count(cs)])\n    JuMP.@objective(model, JuMP.MAX_SENSE, C.substitute(objective, x))\n    C.traverse(cs) do c\n        b = c.bound\n        if b isa C.EqualTo\n            JuMP.@constraint(model, C.substitute(c.value, x) == b.equal_to)\n        elseif b isa C.Between\n            val = C.substitute(c.value, x)\n            isinf(b.lower) || JuMP.@constraint(model, val >= b.lower)\n            isinf(b.upper) || JuMP.@constraint(model, val <= b.upper)\n        end\n    end\n    JuMP.optimize!(model)\n    JuMP.value.(model[:x])\nend\n\nimport GLPK\noptimal_variable_assignment = optimized_vars(s, s.profit.value, GLPK.Optimizer)\n\nThis gives us the optimized variable values! If we cared to remember what they stand for, we might already know how much barley to sow. On the other hand, the main point of ConstraintTree.jl is that one should not be forced to remember things like variable ordering and indexes, or be forced to manually calculate how much money we actually make or how much fertilizer is going to be left – instead, we can simply feed the variable values back to the constraint tree, and get a really good overview of all values in our constrained system:\n\noptimal_s = C.substitute_values(s, optimal_variable_assignment)","title":"Solving the system with JuMP"},{"category":"section","location":"0-quickstart/#Browsing-the-result","page":"Quick start","text":"optimal_s is now like the original constraint tree, just the contents are \"plain old values\" instead of the constraints as above. Thus we can easily see our profit:\n\noptimal_s.profit\n\n...as well as the occupied area for each crop:\n\noptimal_s.area\n\n...and the consumed resources:\n\noptimal_s.resources\n\nPretty-printing works also for trees with results:\n\nC.pretty(optimal_s)","title":"Browsing the result"},{"category":"section","location":"0-quickstart/#Increasing-the-complexity","page":"Quick start","text":"A crucial property of constraint trees is that the users do not need to care about what kind of value they are manipulating – no matter if something is a variable or a derived value, the code that works with it is the same. For example, we can use the actual prices for our resources (30🪙 and 110🪙 for a kilo of fertilizer and insecticide, respectively) to make a corrected profit:\n\ns *=\n    :actual_profit^C.Constraint(\n        s.profit.value - 30 * s.resources.fertilizer.value -\n        110 * s.resources.insecticide.value,\n    )\n\nIs the result going to change if we optimize for the corrected profit?\n\nrealistically_optimal_s =\n    C.substitute_values(s, optimized_vars(s, s.actual_profit.value, GLPK.Optimizer))\n\nrealistically_optimal_s.area","title":"Increasing the complexity"},{"category":"section","location":"0-quickstart/#Combining-constraint-systems:-Let's-have-a-factory!","page":"Quick start","text":"The second crucial property of constraint trees is the ability to easily combine different constraint systems into one. Let's pretend we also somehow obtained a food factory that produces malty sweet bread and wheaty weizen-style beer, with various extra consumptions of water and heat for each of the products. For simplicity, let's just create the corresponding constraint system (f as a factory) here:\n\nf = :products^C.variables(keys = [:bread, :weizen], bounds = C.Between(0, Inf))\nf *= :profit^C.Constraint(25 * f.products.weizen.value + 35 * f.products.bread.value)\n\nWe can make the constraint systems more complex by adding additional variables. To make sure the variables do not \"conflict\", one must use the + operator. While constraint systems combined with * always share variables, constraint systems combined with + are independent.\n\nf += :materials^C.variables(keys = [:wheat, :barley], bounds = C.Between(0, Inf))\n\nHow much resources are consumed by each product, with a limit on each:\n\nf *=\n    :resources^C.ConstraintTree(\n        :heat => C.Constraint(\n            5 * f.products.bread.value + 3 * f.products.weizen.value,\n            (0, 1000),\n        ),\n        :water => C.Constraint(\n            2 * f.products.bread.value + 10 * f.products.weizen.value,\n            (0, 3000),\n        ),\n    )\n\nHow much raw materials are required for each product:\n\nf *=\n    :material_allocation^C.ConstraintTree(\n        :wheat => C.Constraint(\n            8 * f.products.bread.value + 2 * f.products.weizen.value -\n            f.materials.wheat.value,\n            0,\n        ),\n        :barley => C.Constraint(\n            0.5 * f.products.bread.value + 10 * f.products.weizen.value -\n            f.materials.barley.value,\n            0,\n        ),\n    )\n\nHaving the two systems at hand, we can connect the factory \"system\" f to the field \"system\" s, making a compound system c as such:\n\nc = :factory^f + :fields^s\n\nwarning: Operators for combining constraint trees\nAlways remember to use + instead of * when combining independent constraint systems. If we use *, the variables in both systems will become implicitly shared, which is rarely what one wants in the first place. Use * only if adding additional constraints to an existing system. As a rule of thumb, one can remember the boolean interpretation of * as \"and\" and of + as \"or\".On a side note, the operator ^ was chosen mainly to match the algebraic view of the tree combination, and nicely fit into Julia's operator priority structure.\n\nTo actually connect the systems (which now exist as completely independent parts of s), let's add a transport – the barley and wheat produced on the fields is going to be the only barley and wheat consumed by the factory, thus their production and consumption must sum to net zero:\n\nc *= :transport^C.zip(c.fields.area, c.factory.materials) do area, material\n    C.Constraint(area.value - material.value, 0)\nend\n\ninfo: High-level constraint tree manipulation\nThere is also a dedicated example with many more useful functions like zip above.\n\nThe complete system (with a slightly humanized variable formatting added for legibility) now looks like this:\n\nC.pretty(c, format_variable = i -> i == 0 ? \"\" : \" \" * ('a' + i - 1))\n\nFinally, let's see how much money can we make from having the factory supported by our fields in total!\n\noptimal_c =\n    C.substitute_values(c, optimized_vars(c, c.factory.profit.value, GLPK.Optimizer))\n\nHow much field area did we allocate?\n\noptimal_c.fields.area\n\nHow much of each of the products does the factory make in the end?\n\noptimal_c.factory.products\n\nHow much extra resources is consumed by the factory?\n\noptimal_c.factory.resources\n\nAnd what is the factory profit in the end?\n\noptimal_c.factory.profit\n\n\n\nThis page was generated using Literate.jl.","title":"Combining constraint systems: Let's have a factory!"},{"category":"section","location":"2-quadratic-optimization/#Example:-Quadratic-optimization","page":"Example: Quadratic optimization","text":"In this example we demonstrate the use of quadratic constraints and values. We assume that the reader is already familiar with the construction of ConstraintTrees; if not, it is advisable to read the previous part of the documentation first.\n\nIn short, quadratic values and constraints are expressed similarly as other contents of the constraint trees using type QuadraticValue, which is basically an affine-quadratic alike of the affine-linear LinearValue.","title":"Example: Quadratic optimization"},{"category":"section","location":"2-quadratic-optimization/#Working-with-quadratic-values-and-constraints","page":"Example: Quadratic optimization","text":"Algebraically, you can construct QuadraticValues simply by multiplying the linear LinearValues:\n\nimport ConstraintTrees as C\n\nsystem = C.variables(keys = [:x, :y, :z])\nqv = system.x.value * (system.y.value + 2 * system.z.value)\n\nAs with LinearValues, the QuadraticValues can be easily combined, giving a nice way to specify e.g. weighted sums of squared errors with respect to various directions. We can thus represent common formulas for error values:\n\nerror_val =\n    C.squared(system.x.value + system.y.value - 1) +\n    C.squared(system.y.value + 5 * system.z.value - 3)\n\nThis allows us to naturally express quadratic constraint (e.g., that an error must not be too big); and directly observe the error values in the system.\n\nsystem = :vars^system * :error^C.Constraint(error_val, C.Between(0, 100))\n\n(For simplicity, you can also use the Constraint constructor to make quadratic constraints out of QuadraticValues – it will overload properly.)\n\nLet's pretend someone has solved the system, and see how much \"error\" the solution has:\n\nsolution = [1.0, 2.0, -1.0]\nst = C.substitute_values(system, solution)\nst.error\n\n...not bad for a first guess.","title":"Working with quadratic values and constraints"},{"category":"section","location":"2-quadratic-optimization/#Building-quadratic-systems","page":"Example: Quadratic optimization","text":"Let's create a small quadratic system that finds the closest distance between an ellipse and a line and let some of the conic solvers available in JuMP solve it. First, let's make a representation of a point in 2D:\n\npoint = C.variables(keys = [:x, :y])\n\nWe can create a small system that constraints the point to stay within a simple elliptical area centered around (0.0, 10.0):\n\nellipse_system = C.ConstraintTree(\n    :point => point,\n    :in_area => C.Constraint(\n        C.squared(point.x.value) / 4 + C.squared(10.0 - point.y.value),\n        C.Between(-Inf, 1.0),\n    ),\n)\n\nWe now create another small system that constraints another point to stay on a line that crosses (0, 0) and (2, 1). We could do this using a dot-product representation of line, but that would lead to issues later (mainly, the solver that we are planning to use only supports positive definite quadratic forms as constraints). Instead, let's use a single-variable-parametrized line equation.\n\nline_param = C.variable().value\nline_system =\n    :point^C.ConstraintTree([:x, :y] .=> C.Constraint.([0, 0] .+ [2, 1] .* line_param))\n\nFinally, let's connect the systems using + operator and add the objective that would minimize the distance of the points:\n\ns = :ellipse^ellipse_system + :line^line_system\n\ns *=\n    :objective^C.Constraint(\n        C.squared(s.ellipse.point.x.value - s.line.point.x.value) +\n        C.squared(s.ellipse.point.y.value - s.line.point.y.value),\n    )\n\n(Note that if we used * to connect the systems, the variables from the definition of point would not be duplicated, and various non-interesting logic errors would follow.)\n\nThe complete system now looks like this:\n\nC.pretty(s; format_label = x -> join(x, '.'))\n\n(We changed the tree formatting to show full paths, for clarity.)","title":"Building quadratic systems"},{"category":"section","location":"2-quadratic-optimization/#Solving-quadratic-systems-with-JuMP","page":"Example: Quadratic optimization","text":"To solve the above system, we need a matching solver that can work with quadratic constraints. Also, we need to slightly generalize the function that translates the constraints into JuMP Models to support the quadratic constraints.\n\nimport JuMP\nfunction quad_optimized_vars(cs::C.ConstraintTree, objective::C.Value, optimizer)\n    model = JuMP.Model(optimizer)\n    JuMP.@variable(model, x[1:C.variable_count(cs)])\n    JuMP.@objective(model, JuMP.MAX_SENSE, C.substitute(objective, x))\n    C.traverse(cs) do c\n        b = c.bound\n        if b isa C.EqualTo\n            JuMP.@constraint(model, C.substitute(c.value, x) == b.equal_to)\n        elseif b isa C.Between\n            val = C.substitute(c.value, x)\n            isinf(b.lower) || JuMP.@constraint(model, val >= b.lower)\n            isinf(b.upper) || JuMP.@constraint(model, val <= b.upper)\n        end\n    end\n    JuMP.set_silent(model)\n    JuMP.optimize!(model)\n    JuMP.value.(model[:x])\nend\n\nWe can now load a suitable optimizer and solve the system by maximizing the negative squared error:\n\nimport Clarabel\nst = C.substitute_values(s, quad_optimized_vars(s, -s.objective.value, Clarabel.Optimizer))\n\nIf the optimization worked well, we can nicely get out the position of the closest point to the line that is in the elliptical area:\n\n(st.ellipse.point.x, st.ellipse.point.y)\n\n...as well as the position on the line that is closest to the ellipse:\n\nst.line.point\n\n...and, with a little bit of extra math, the minimized distance:\n\nsqrt(st.objective)\n\n\n\nThis page was generated using Literate.jl.","title":"Solving quadratic systems with JuMP"},{"category":"section","location":"6-io/#Exporting-and-importing-constraint-systems","page":"Exporting and importing constraint systems","text":"To ease communication with other software, it is possible to export constraint trees to a file and re-import them. That may serve various purposes:\n\nyou can exchange the systems in constraint trees more easily than by sharing the \"builder\" scripts, and more reliably and portably than with Julia's general serialization capabilities (in package Serialization)\nyou can exchange the system with different software (and different programming environments) which may e.g. implement other solving and analysis methods\nyou may separate your software into a tree-building and tree-solving part, both more lightweight and more suitable to use in resource-constrained environments (such as on HPCs).\n\nThe package exports functions serialize and deserialize that convert the constraint trees to and from Julia containers (dictionaries and vectors that only contain simple data such as strings and numbers). These can be further converted to JSON or other data exchange formats.\n\nFor demonstration, we are going to make a simple constraint tree:\n\nimport ConstraintTrees as C\n\nt = C.variables(keys = [:x, :y], bounds = C.Between(-10, 10))\nt =\n    :point^t *\n    :difference^C.Constraint(t.x.value - t.y.value, C.EqualTo(3)) *\n    :length^C.Constraint(C.squared(t.x.value) + C.squared(t.y.value))\n\nIn the \"nice formatting\" the tree looks as follows:\n\nC.pretty(t)","title":"Exporting and importing constraint systems"},{"category":"section","location":"6-io/#Serialization","page":"Exporting and importing constraint systems","text":"Serialization converts the tree to a Dict with appropriate contents:\n\nC.serialize(t)\n\nThe arguably easiest way to \"export\" the serialized tree is to write the Dict out as JSON:\n\nimport JSON\n\n    JSON.print(C.serialize(t), 2)\n\n...and with appropriate functions, save the JSON to disk:\n\nopen(\"ct-test.json\", \"w\") do f\n    JSON.print(f, C.serialize(t))\nend","title":"Serialization"},{"category":"section","location":"6-io/#De-serialization","page":"Exporting and importing constraint systems","text":"Because of the involved polymorphism, deserialization functions need to know the type of what is actually being parsed, and we also have to specify that the JSON parsing should output an actual Dict. With that in hand, the JSON can be re-loaded as follows, giving the same tree:\n\nt2 = C.deserialize(\n    C.ConstraintTree,\n    JSON.parsefile(\"ct-test.json\", dicttype = Dict{String,Any}),\n)\n\nC.pretty(t2)\n\n\n\nThis page was generated using Literate.jl.","title":"De-serialization"},{"category":"section","location":"reference/#Reference","page":"Reference","text":"","title":"Reference"},{"category":"section","location":"reference/#Values","page":"Reference","text":"","title":"Values"},{"category":"section","location":"reference/#Linear-and-affine-values","page":"Reference","text":"","title":"Linear and affine values"},{"category":"section","location":"reference/#Quadratic-values","page":"Reference","text":"","title":"Quadratic values"},{"category":"section","location":"reference/#Constraints","page":"Reference","text":"","title":"Constraints"},{"category":"section","location":"reference/#Bounds","page":"Reference","text":"","title":"Bounds"},{"category":"section","location":"reference/#Constrained-values","page":"Reference","text":"","title":"Constrained values"},{"category":"section","location":"reference/#Labeled-trees","page":"Reference","text":"","title":"Labeled trees"},{"category":"section","location":"reference/#Constraint-trees","page":"Reference","text":"","title":"Constraint trees"},{"category":"section","location":"reference/#Serialization","page":"Reference","text":"","title":"Serialization"},{"category":"section","location":"reference/#Pretty-printing","page":"Reference","text":"","title":"Pretty-printing"},{"category":"type","location":"reference/#ConstraintTrees.Value","page":"Reference","text":"abstract type Value\n\nAbstract type of all values usable in constraints, including LinearValue and QuadraticValue.\n\nAll Values are broadcastable as scalars by default.\n\n\n\n\n\n","title":"ConstraintTrees.Value"},{"category":"method","location":"reference/#ConstraintTrees.preduce-Tuple{Any, Any}","page":"Reference","text":"preduce(op, xs; init, stack_type) -> Any\n\n\nAn alternative of Base.reduce which does a \"pairwise\" reduction in the shape of a binary merge tree, like in mergesort. In general this is a little more complex, but if the reduced value \"grows\" with more elements added (such as when adding a lot of LinearValues together), this is able to prevent a complexity explosion by postponing \"large\" reducing operations as much as possible.\n\nIn the specific case with adding lots of LinearValues and QuadraticValues together, this effectively squashes the reduction complexity from something around O(n^2) to O(n) (with a little larger constant factor.\n\n\n\n\n\n","title":"ConstraintTrees.preduce"},{"category":"function","location":"reference/#ConstraintTrees.substitute_values","page":"Reference","text":"substitute_values(x::Value, y::AbstractVector) -> Any\nsubstitute_values(x::Value, y::AbstractVector, _) -> Any\n\n\nSubstutite a value into a Value-typed x. This is a convenience overload for the purpose of having substitute_values to run on both Constraints and Values.\n\n\n\n\n\n","title":"ConstraintTrees.substitute_values"},{"category":"method","location":"reference/#ConstraintTrees.sum-Tuple{Any}","page":"Reference","text":"sum(xs; init) -> Any\n\n\nAlias for preduce that uses + as the operation.\n\nNot as versatile as the sum from Base, but much faster for growing values like LinearValues and QuadraticValues.\n\n\n\n\n\n","title":"ConstraintTrees.sum"},{"category":"method","location":"reference/#ConstraintTrees.value-Tuple{T} where T<:Union{Value, Real}","page":"Reference","text":"value(x::Union{Value, Real}) -> Union{Value, Real}\n\n\nReturns any Real- or Value-typed x. This is a convenience overload; typically one enjoys this more when extracting values from Constraints.\n\n\n\n\n\n","title":"ConstraintTrees.value"},{"category":"type","location":"reference/#ConstraintTrees.LinearValue","page":"Reference","text":"struct LinearValueT{Float64} <: Value\n\nA shortcut for a LinearValueT over Float64s.\n\n\n\n\n\n","title":"ConstraintTrees.LinearValue"},{"category":"method","location":"reference/#ConstraintTrees.LinearValue-Tuple{Real}","page":"Reference","text":"LinearValue(x::Real) -> LinearValueT\n\n\nConstruct a constant-valued LinearValue with a single affine element.\n\n\n\n\n\n","title":"ConstraintTrees.LinearValue"},{"category":"method","location":"reference/#ConstraintTrees.LinearValue-Tuple{SparseArrays.SparseVector{Float64}}","page":"Reference","text":"LinearValue(\n    x::SparseArrays.SparseVector{Float64}\n) -> LinearValue\n\n\nShortcut for making a LinearValue out of a linear combination defined by the SparseVector.\n\n\n\n\n\n","title":"ConstraintTrees.LinearValue"},{"category":"type","location":"reference/#ConstraintTrees.LinearValueT","page":"Reference","text":"struct LinearValueT{T} <: Value\n\nA representation of a \"value\" in a linear constrained optimization problem. The value is an affine linear combination of several variables, weighted by coefficients of the parameter type T.\n\nLinearValueTs can be combined additively and multiplied by real-number constants.  Multiplying two LinearValueTs yields a quadratic form (in a QuadraticValueT).\n\nFields\n\nidxs::Vector{Int64}: Indexes of the variables used by the value. The indexes must always be sorted in strictly increasing order. The affine element has index 0.\n\nweights::Vector: Coefficients of the variables selected by idxs.\n\n\n\n\n\n","title":"ConstraintTrees.LinearValueT"},{"category":"method","location":"reference/#ConstraintTrees.LinearValueT-Tuple{R} where R<:Real","page":"Reference","text":"LinearValueT(x::Real) -> LinearValueT{R} where R<:Real\n\n\nConstruct a constant-valued LinearValueT with a single affine element.\n\n\n\n\n\n","title":"ConstraintTrees.LinearValueT"},{"category":"method","location":"reference/#ConstraintTrees.LinearValueT-Union{Tuple{SparseArrays.SparseVector{T}}, Tuple{T}} where T","page":"Reference","text":"LinearValueT(\n    x::SparseArrays.SparseVector{T}\n) -> LinearValueT\n\n\nGeneralized constructor for LinearValueTs from sparse vectors.\n\n\n\n\n\n","title":"ConstraintTrees.LinearValueT"},{"category":"method","location":"reference/#ConstraintTrees.add_sparse_linear_combination-Union{Tuple{T}, Tuple{Vector{Int64}, Vector{T}, Vector{Int64}, Vector{T}}} where T","page":"Reference","text":"add_sparse_linear_combination(\n    a_idxs::Vector{Int64},\n    a_weights::Array{T, 1},\n    b_idxs::Vector{Int64},\n    b_weights::Array{T, 1}\n) -> Tuple{Vector{Int64}, Any}\n\n\nHelper function for implementing LinearValue-like objects. Given \"sparse\" representations of linear combinations, it computes a \"merged\" linear combination of 2 values added together.\n\nZeroes are not filtered out.\n\n\n\n\n\n","title":"ConstraintTrees.add_sparse_linear_combination"},{"category":"method","location":"reference/#ConstraintTrees.substitute-Tuple{LinearValueT, Any}","page":"Reference","text":"substitute(x::LinearValueT, y) -> Any\n\n\nSubstitute anything vector-like as variable values into a LinearValueT and return the result.\n\n\n\n\n\n","title":"ConstraintTrees.substitute"},{"category":"type","location":"reference/#ConstraintTrees.QuadraticValue","page":"Reference","text":"struct QuadraticValueT{Float64} <: Value\n\nA shortcut for a QuadraticValueT over Float64s.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValue"},{"category":"method","location":"reference/#ConstraintTrees.QuadraticValue-Tuple{LinearValue}","page":"Reference","text":"QuadraticValue(x::LinearValue) -> QuadraticValue\n\n\nConstruct a QuadraticValue that is equivalent to a given LinearValue.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValue"},{"category":"method","location":"reference/#ConstraintTrees.QuadraticValue-Tuple{Real}","page":"Reference","text":"QuadraticValue(x::Real) -> QuadraticValueT\n\n\nConstruct a constant-valued QuadraticValue with a single affine element.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValue"},{"category":"method","location":"reference/#ConstraintTrees.QuadraticValue-Tuple{SparseArrays.SparseMatrixCSC{Float64}}","page":"Reference","text":"QuadraticValue(\n    x::SparseArrays.SparseMatrixCSC{Float64}\n) -> QuadraticValue\n\n\nShortcut for making a QuadraticValue out of a square sparse matrix.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValue"},{"category":"type","location":"reference/#ConstraintTrees.QuadraticValueT","page":"Reference","text":"struct QuadraticValueT{T} <: Value\n\nA representation of a quadratic form in the constrained optimization problem. The QuadraticValue is an affine quadratic combination (i.e., a multinomial of maximum degree 2) over the variables, weighted by coefficients of the parameter type T.\n\nQuadraticValues can be combined additively and multiplied by real-number constants. The cleanest way to construct a QuadraticValue is to multiply two LinearValues.\n\nFields\n\nidxs::Vector{Tuple{Int64, Int64}}: Indexes of variable pairs used by the value. The indexes must always be sorted in strictly co-lexicographically increasing order, and the first index must always be less than or equal to the second one. (Speaking in matrix terms, the indexing follows the indexes in an upper triangular matrix by columns.)\nAs an outcome, the second index of the last index pair can be used as the upper bound of all variable indexes.\nAs with LinearValueT, index 0 represents the affine element.\n\nweights::Vector: Coefficient of the variable pairs selected by idxs.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValueT"},{"category":"method","location":"reference/#ConstraintTrees.QuadraticValueT-Tuple{LinearValueT}","page":"Reference","text":"QuadraticValueT(x::LinearValueT) -> QuadraticValueT\n\n\nConstruct a QuadraticValueT that is equivalent to a given LinearValueT.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValueT"},{"category":"method","location":"reference/#ConstraintTrees.QuadraticValueT-Tuple{R} where R<:Real","page":"Reference","text":"QuadraticValueT(x::Real) -> QuadraticValueT{R} where R<:Real\n\n\nConstruct a constant-valued QuadraticValueT with a single affine element.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValueT"},{"category":"method","location":"reference/#ConstraintTrees.QuadraticValueT-Union{Tuple{SparseArrays.SparseMatrixCSC{T}}, Tuple{T}} where T","page":"Reference","text":"QuadraticValueT(\n    x::SparseArrays.SparseMatrixCSC{T}\n) -> QuadraticValue\n\n\nGeneralized constructor for QuadraticValueT from square sparse matrices.\n\nThe matrix is force-symmetrized by calculating x' + x.\n\n\n\n\n\n","title":"ConstraintTrees.QuadraticValueT"},{"category":"method","location":"reference/#ConstraintTrees.add_sparse_quadratic_combination-Union{Tuple{T}, Tuple{Vector{Tuple{Int64, Int64}}, Vector{T}, Vector{Tuple{Int64, Int64}}, Vector{T}}} where T","page":"Reference","text":"add_sparse_quadratic_combination(\n    a_idxs::Vector{Tuple{Int64, Int64}},\n    a_weights::Array{T, 1},\n    b_idxs::Vector{Tuple{Int64, Int64}},\n    b_weights::Array{T, 1}\n) -> Tuple{Vector{Tuple{Int64, Int64}}, Any}\n\n\nHelper function for implementing QuadraticValue-like objects. Given 2 sparse representations of quadratic combinations, it computes a \"merged\" one with the values of both added together.\n\nZeroes are not filtered out.\n\n\n\n\n\n","title":"ConstraintTrees.add_sparse_quadratic_combination"},{"category":"method","location":"reference/#ConstraintTrees.colex_lt-Tuple{Any, Any}","page":"Reference","text":"colex_lt(, ) -> Any\n\n\nInternal helper for co-lex ordering of indexes.\n\n\n\n\n\n","title":"ConstraintTrees.colex_lt"},{"category":"method","location":"reference/#ConstraintTrees.multiply_sparse_linear_combination-Union{Tuple{T}, Tuple{Vector{Int64}, Vector{T}, Vector{Int64}, Vector{T}}} where T","page":"Reference","text":"multiply_sparse_linear_combination(\n    a_idxs::Vector{Int64},\n    a_weights::Array{T, 1},\n    b_idxs::Vector{Int64},\n    b_weights::Array{T, 1}\n) -> Tuple{Vector{Tuple{Int64, Int64}}, Any}\n\n\nHelper function for multiplying two LinearValue-like objects to make a QuadraticValue-like object. This computes and merges the product.\n\nZeroes are not filtered out.\n\n\n\n\n\n","title":"ConstraintTrees.multiply_sparse_linear_combination"},{"category":"method","location":"reference/#ConstraintTrees.squared-Tuple{LinearValueT}","page":"Reference","text":"squared(a::LinearValueT) -> QuadraticValueT\n\n\nBroadcastable shortcut for multiplying a LinearValueT with itself. Produces a QuadraticValueT.\n\n\n\n\n\n","title":"ConstraintTrees.squared"},{"category":"method","location":"reference/#ConstraintTrees.substitute-Tuple{QuadraticValueT, Any}","page":"Reference","text":"substitute(x::QuadraticValueT, y) -> Any\n\n\nSubstitute anything vector-like as variable values into the QuadraticValueT and return the result.\n\n\n\n\n\n","title":"ConstraintTrees.substitute"},{"category":"type","location":"reference/#ConstraintTrees.MaybeBound","page":"Reference","text":"Shortcut for all possible Bounds including the \"empty\" bound that does not constraint anything (represented by nothing).\n\n\n\n\n\n","title":"ConstraintTrees.MaybeBound"},{"category":"type","location":"reference/#ConstraintTrees.Between","page":"Reference","text":"mutable struct BetweenT{Float64} <: Bound\n\nShortcut for a Float64-typed interval bound implemented by BetweenT.\n\n\n\n\n\n","title":"ConstraintTrees.Between"},{"category":"method","location":"reference/#ConstraintTrees.Between-Tuple{Real, Real}","page":"Reference","text":"Between(x::Real, y::Real) -> BetweenT\n\n\nConstruct a Between bound. Additionally, this checks the order of the values and puts them into a correct order.\n\n\n\n\n\n","title":"ConstraintTrees.Between"},{"category":"type","location":"reference/#ConstraintTrees.BetweenT","page":"Reference","text":"mutable struct BetweenT{T} <: Bound\n\nRepresentation of an \"interval\" bound; consisting of lower and upper bound value.\n\nFields\n\nlower::Any: Lower bound\nupper::Any: Upper bound\n\n\n\n\n\n","title":"ConstraintTrees.BetweenT"},{"category":"type","location":"reference/#ConstraintTrees.Bound","page":"Reference","text":"abstract type Bound\n\nAbstract type of all bounds usable in constraints, including Between and EqualTo.\n\nAll Bounds are broadcastable as scalars by default.\n\n\n\n\n\n","title":"ConstraintTrees.Bound"},{"category":"type","location":"reference/#ConstraintTrees.EqualTo","page":"Reference","text":"mutable struct EqualToT{Float64} <: Bound\n\nShortcut for a Float64-typed equality bound implemented by EqualToT.\n\n\n\n\n\n","title":"ConstraintTrees.EqualTo"},{"category":"method","location":"reference/#ConstraintTrees.EqualTo-Tuple{Real}","page":"Reference","text":"EqualTo(x::Real) -> EqualToT\n\n\nConstruct an EqualTo bound.\n\n\n\n\n\n","title":"ConstraintTrees.EqualTo"},{"category":"type","location":"reference/#ConstraintTrees.EqualToT","page":"Reference","text":"mutable struct EqualToT{T} <: Bound\n\nRepresentation of an \"equality\" bound, which contains the single \"equal to this\" value.\n\nFields\n\nequal_to::Any: Equality bound value\n\n\n\n\n\n","title":"ConstraintTrees.EqualToT"},{"category":"method","location":"reference/#Base.length-Tuple{Bound}","page":"Reference","text":"length(x::Bound) -> Int64\n\n\nDeprecation warning: This is kept for backwards compatibility only, and will be removed in a future release.\n\n\n\n\n\n","title":"Base.length"},{"category":"type","location":"reference/#ConstraintTrees.Constraint","page":"Reference","text":"mutable struct Constraint\n\nA representation of a single constraint that may limit the given value by a specific Bound.\n\nConstraints without a bound (nothing in the bound field) are possible; these have no impact on the optimization problem but the associated value becomes easily accessible for inspection and building other constraints.\n\nFields\n\nvalue::Value: A value (typically a LinearValue or a QuadraticValue)     that describes what the constraint constraints.\nbound::Union{Nothing, Bound}: A bound that the value must satisfy. Should be a subtype of     MaybeBound: Either nothing if there's no bound, or e.g.     EqualToT, BetweenT or similar structs.\n\n\n\n\n\n","title":"ConstraintTrees.Constraint"},{"category":"method","location":"reference/#ConstraintTrees.bound-Tuple{Constraint}","page":"Reference","text":"bound(x::Constraint) -> Union{Nothing, Bound}\n\n\nSimple accessor for getting out the bound from the constraint that can be used for broadcasting (as opposed to the dot-field access).\n\n\n\n\n\n","title":"ConstraintTrees.bound"},{"category":"method","location":"reference/#ConstraintTrees.substitute-Tuple{Constraint, Any}","page":"Reference","text":"substitute(x::Constraint, y) -> Constraint\n\n\nSubstitute anything vector-like as variables into the constraint's value, producing a constraint with the new value.\n\n\n\n\n\n","title":"ConstraintTrees.substitute"},{"category":"function","location":"reference/#ConstraintTrees.substitute_values-2","page":"Reference","text":"substitute_values(x::Constraint, y::AbstractVector) -> Any\nsubstitute_values(\n    x::Constraint,\n    y::AbstractVector,\n    _\n) -> Any\n\n\nOverload of substitute_values for a single constraint.\n\n\n\n\n\n","title":"ConstraintTrees.substitute_values"},{"category":"method","location":"reference/#ConstraintTrees.value-Tuple{Constraint}","page":"Reference","text":"value(x::Constraint) -> Value\n\n\nSimple accessor for getting out the value from the constraint that can be used for broadcasting (as opposed to the dot-field access).\n\n\n\n\n\n","title":"ConstraintTrees.value"},{"category":"type","location":"reference/#ConstraintTrees.OptionalTree","page":"Reference","text":"Helper type for implementation of merge-related functions.\n\n\n\n\n\n","title":"ConstraintTrees.OptionalTree"},{"category":"type","location":"reference/#ConstraintTrees.Tree","page":"Reference","text":"struct Tree{X}\n\nA base \"labeled tree\" structure. Supports many interesting operations such as merging.\n\n\n\n\n\n","title":"ConstraintTrees.Tree"},{"category":"method","location":"reference/#ConstraintTrees.deflate-Union{Tuple{U}, Tuple{T}, Tuple{Any, Tree{T}}, Tuple{Any, Tree{T}, Type{U}}} where {T, U}","page":"Reference","text":"deflate(f, x::Tree{T}) -> Any\ndeflate(f, x::Tree{T}, ::Type{U}) -> Any\n\n\nExtract all elements of a Tree in order, and return them in a vector as transformed by f. If the order is not changed, one can re-insert a vector of modified elements into the same-shaped tree using reinflate.\n\n\n\n\n\n","title":"ConstraintTrees.deflate"},{"category":"method","location":"reference/#ConstraintTrees.elems-Tuple{Tree}","page":"Reference","text":"elems(\n    x::Tree\n) -> DataStructures.SortedDict{Symbol, Union{Tree{X}, X}} where X\n\n\nGet the elements dictionary out of the Tree. This is useful for getting an iterable container for working with many items at once.\n\nAlso, because of the overload of getproperty for Tree, this serves as a simpler way to get the elements without an explicit use of getfield.\n\n\n\n\n\n","title":"ConstraintTrees.elems"},{"category":"method","location":"reference/#ConstraintTrees.filter-Union{Tuple{T}, Tuple{Any, Tree{T}}} where T","page":"Reference","text":"filter(f, x::Tree{T}) -> Any\n\n\nFilter all branches and leaves in a tree, leaving only the ones where f returns true.\n\nNote that the branches are passed to f as well. Use filter_leaves to only work with the leaf values.\n\n\n\n\n\n","title":"ConstraintTrees.filter"},{"category":"method","location":"reference/#ConstraintTrees.filter_leaves-Union{Tuple{T}, Tuple{Any, Tree{T}}} where T","page":"Reference","text":"filter_leaves(f, x::Tree{T}) -> Any\n\n\nLike filter but the filtering predicate function f only receives the leaf values (i.e., no intermediate sub-trees).\n\nIn turn, the result will retain the whole subtree structure (even if empty).\n\n\n\n\n\n","title":"ConstraintTrees.filter_leaves"},{"category":"method","location":"reference/#ConstraintTrees.ideflate-Union{Tuple{U}, Tuple{T}, Tuple{Any, Tree{T}}, Tuple{Any, Tree{T}, Type{U}}} where {T, U}","page":"Reference","text":"ideflate(f, x::Tree{T}) -> Any\nideflate(f, x::Tree{T}, ::Type{U}) -> Any\n\n\nLike deflate, but the function f also receives the full tree path in the first argument.\n\n\n\n\n\n","title":"ConstraintTrees.ideflate"},{"category":"method","location":"reference/#ConstraintTrees.ifilter-Union{Tuple{T}, Tuple{Any, Tree{T}}} where T","page":"Reference","text":"ifilter(f, x::Tree{T}) -> Any\n\n\nLike filter but the filtering predicate function also receives the \"path\" in the tree.\n\n\n\n\n\n","title":"ConstraintTrees.ifilter"},{"category":"method","location":"reference/#ConstraintTrees.ifilter_leaves-Union{Tuple{T}, Tuple{Any, Tree{T}}} where T","page":"Reference","text":"ifilter_leaves(f, x::Tree{T}) -> Any\n\n\nCombination of ifilter and filter_leaves.\n\n\n\n\n\n","title":"ConstraintTrees.ifilter_leaves"},{"category":"method","location":"reference/#ConstraintTrees.imap-Union{Tuple{T}, Tuple{Any, Any}, Tuple{Any, Any, Type{T}}} where T","page":"Reference","text":"imap(f, x) -> Any\nimap(f, x, ::Type{T}) -> Any\n\n\nLike map, but keeping the \"index\" path and giving it to the function as the first parameter. The \"path\" in the tree is reported as a tuple of symbols.\n\n\n\n\n\n","title":"ConstraintTrees.imap"},{"category":"method","location":"reference/#ConstraintTrees.imapreduce-Tuple{Any, Any, Any}","page":"Reference","text":"imapreduce(f, op, x; init) -> Any\n\n\nLike mapreduce but reporting the \"tree directory path\" where the reduced elements occur, like with imap. (Single elements from different directory paths are not reduced together.)\n\n\n\n\n\n","title":"ConstraintTrees.imapreduce"},{"category":"method","location":"reference/#ConstraintTrees.imerge-Union{Tuple{T}, Tuple{Any, Any, Any}, Tuple{Any, Any, Any, Type{T}}} where T","page":"Reference","text":"imerge(f, x, y) -> Any\nimerge(f, x, y, ::Type{T}) -> Any\n\n\nIndex-reporting variant of merge (see imap for reference).\n\n\n\n\n\n","title":"ConstraintTrees.imerge"},{"category":"method","location":"reference/#ConstraintTrees.ireduce-Tuple{Any, Any}","page":"Reference","text":"ireduce(op, x; init) -> Any\n\n\nIndexed version of reduce (internally uses imapreduce).\n\n\n\n\n\n","title":"ConstraintTrees.ireduce"},{"category":"method","location":"reference/#ConstraintTrees.itraverse-Tuple{Any, Any}","page":"Reference","text":"itraverse(f, x) -> Any\n\n\nitraverse is to traverse like imap is to map.\n\n\n\n\n\n","title":"ConstraintTrees.itraverse"},{"category":"method","location":"reference/#ConstraintTrees.izip-Union{Tuple{T}, Tuple{Any, Any, Any}, Tuple{Any, Any, Any, Type{T}}} where T","page":"Reference","text":"izip(f, x, y) -> Any\nizip(f, x, y, ::Type{T}) -> Any\n\n\nIndex-reporting variant of zip (see imap for reference).\n\n\n\n\n\n","title":"ConstraintTrees.izip"},{"category":"method","location":"reference/#ConstraintTrees.map-Union{Tuple{T}, Tuple{Any, Any}, Tuple{Any, Any, Type{T}}} where T","page":"Reference","text":"map(f, x) -> Any\nmap(f, x, ::Type{T}) -> Any\n\n\nRun a function over everything in the tree. The resulting tree will contain elements of type specified by the 3rd argument. (This needs to be specified explicitly, because the typesystem generally cannot guess the universal type correctly.)\n\nNote this is a specialized function specific for Trees that behaves differently from Base.map.\n\n\n\n\n\n","title":"ConstraintTrees.map"},{"category":"method","location":"reference/#ConstraintTrees.mapreduce-Tuple{Any, Any, Any}","page":"Reference","text":"mapreduce(f, op, x; init) -> Any\n\n\nReduce all items in a Tree. As with Base.reduce, the reduction order is not guaranteed, and the initial value may be used any number of times.\n\nNote this is a specialized function specific for Trees that behaves differently from Base.mapreduce.\n\n\n\n\n\n","title":"ConstraintTrees.mapreduce"},{"category":"method","location":"reference/#ConstraintTrees.merge-Union{Tuple{T}, Tuple{Any, Any, Any}, Tuple{Any, Any, Any, Type{T}}} where T","page":"Reference","text":"merge(f, x, y) -> Any\nmerge(f, x, y, ::Type{T}) -> Any\n\n\nRun a function over the values in the merge of all paths in the trees (currently there is support for 2 and 3 trees). This is an \"outer join\" equivalent of zip.  Missing elements are replaced by missing in the function call parameters, and the function may return missing to omit elements.\n\nNote this is a specialized function specific for Trees that behaves differently from Base.merge.\n\n\n\n\n\n","title":"ConstraintTrees.merge"},{"category":"method","location":"reference/#ConstraintTrees.optional_tree_get-Tuple{Missing, Any}","page":"Reference","text":"optional_tree_get(_::Missing, _) -> Missing\n\n\nGet a key from a tree that is possibly missing.\n\n\n\n\n\n","title":"ConstraintTrees.optional_tree_get"},{"category":"method","location":"reference/#ConstraintTrees.optional_tree_keys-Tuple{Missing}","page":"Reference","text":"optional_tree_keys(\n    _::Missing\n) -> DataStructures.SortedSet{Symbol, Base.Order.ForwardOrdering}\n\n\nGet a sorted set of keys from a tree that is possibly missing.\n\n\n\n\n\n","title":"ConstraintTrees.optional_tree_keys"},{"category":"method","location":"reference/#ConstraintTrees.reduce-Tuple{Any, Any}","page":"Reference","text":"reduce(op, x; init) -> Any\n\n\nLike mapreduce but the mapped function is identity.\n\nTo avoid much type suffering, the operation should ideally preserve the type of its arguments. If you need to change the type, you likely want to use mapreduce.\n\nNote this is a specialized function specific for Trees that behaves differently from Base.reduce.\n\n\n\n\n\n","title":"ConstraintTrees.reduce"},{"category":"method","location":"reference/#ConstraintTrees.reinflate-Union{Tuple{T}, Tuple{Tree, Vector{T}}} where T","page":"Reference","text":"reinflate(x::Tree, elems::Array{T, 1}) -> Any\n\n\nInsert a vector of elements into the \"values\" of a Tree. The order of elements in the input vector is given by deflate.\n\n\n\n\n\n","title":"ConstraintTrees.reinflate"},{"category":"method","location":"reference/#ConstraintTrees.traverse-Tuple{Any, Any}","page":"Reference","text":"traverse(f, x) -> Any\n\n\nLike map, but discards the results, thus relying only on the side effects of f.\n\nTechnically the name should be for, but that's a Julia keyword.\n\n\n\n\n\n","title":"ConstraintTrees.traverse"},{"category":"method","location":"reference/#ConstraintTrees.zip-Union{Tuple{T}, Tuple{Any, Any, Any}, Tuple{Any, Any, Any, Type{T}}} where T","page":"Reference","text":"zip(f, x, y) -> Any\nzip(f, x, y, ::Type{T}) -> Any\n\n\nRun a function over the values in the intersection of paths in several trees (currently there is support for 2 and 3 trees). This is an \"inner join\" – all extra elements are ignored. \"Outer join\" can be done via merge.\n\nAs with map, the inner type of the resulting tree must be specified by the last parameter..\n\nNote this is a specialized function specific for Trees that behaves differently from Base.zip.\n\n\n\n\n\n","title":"ConstraintTrees.zip"},{"category":"type","location":"reference/#ConstraintTrees.ConstraintTreeElem","page":"Reference","text":"A shortcut for the type of the values in ConstraintTree.\n\n\n\n\n\n","title":"ConstraintTrees.ConstraintTreeElem"},{"category":"type","location":"reference/#ConstraintTrees.ConstraintTree","page":"Reference","text":"struct Tree{Constraint}\n\nA hierarchical tree of many constraints that together describe a constrained system. The tree may recursively contain other trees in a directory-like structure, which contain Constraints as leaves.\n\nMembers of the constraint tree are accessible via the record dot syntax as properties; e.g. a constraint labeled with :abc in a constraint tree t may be accessed as t.abc and as t[:abc], and can be found while iterating through elems(t).\n\nConstructing the constraint trees\n\nUse operator ^ to put a name on a constraint to convert it into a single element ConstraintTree:\n\nx = :my_constraint ^ Constraint(LinearValue(...), 1.0)\ndir = :my_constraint_dir ^ x\n\ndir.my_constraint_dir.my_constraint.bound   # returns 1.0\n\nUse operator * to glue two constraint trees together while sharing the variable indexes specified by the contained LinearValues and QuadraticValues.\n\nmy_constraints = :some_constraints ^ Constraint(...) * :more_constraints ^ Constraint(...)\n\nUse operator + to glue two constraint trees together without sharing of any variables. The operation will renumber the variables in the trees so that the sets of variable indexes used by either tree are completely disjunct, and then glue the trees together as with *:\n\ntwo_independent_systems = my_system + other_system\n\nVariable sharing limitations\n\nBecause of the renumbering, you can not easily use constraints and values from the values before the addition in the constraint tree that is the result of the addition. There is no check against that – the resulting ConstraintTree will be valid, but will probably describe a different optimization problem than you intended.\n\nAs a rule of thumb, avoid necessary parentheses in expressions that work with the constraint trees: While t1 * t2 + t3 might work just as intended, t1 * (t2 + t3) is almost certainly wrong because the variables in t1 that are supposed to connect to variables in either of t2 and t3 will not connect properly because of renumbering of both t2 and t3. If you need to construct a tree like that, do the addition first, and construct the t1 after that, based on the result of the addition.\n\n\n\n\n\n","title":"ConstraintTrees.ConstraintTree"},{"category":"method","location":"reference/#ConstraintTrees.collect_variables!-Tuple{Constraint, Any}","page":"Reference","text":"collect_variables!(x::Constraint, out)\n\n\nPush all variable indexes found in x to the out container.\n\n(The container needs to support the standard push!.)\n\n\n\n\n\n","title":"ConstraintTrees.collect_variables!"},{"category":"method","location":"reference/#ConstraintTrees.collect_variables!-Tuple{Function, Any}","page":"Reference","text":"collect_variables!(out::Function, x) -> Any\n\n\nOverload of collect_variables! that calls a given function with all variable indexes found in x.\n\n\n\n\n\n","title":"ConstraintTrees.collect_variables!"},{"category":"method","location":"reference/#ConstraintTrees.drop_zeros-Union{Tuple{Tree{T}}, Tuple{T}} where T","page":"Reference","text":"drop_zeros(x::Tree{T}) -> ConstraintTree\n\n\nRemove variable references from all Values in the given object (usually a ConstraintTree) where the variable weight is exactly zero.\n\n\n\n\n\n","title":"ConstraintTrees.drop_zeros"},{"category":"function","location":"reference/#ConstraintTrees.incr_var_idxs","page":"Reference","text":"Old name for increase_variable_indexes.\n\nDeprecation warning: This will be removed in a future release.\n\n\n\n\n\n","title":"ConstraintTrees.incr_var_idxs"},{"category":"method","location":"reference/#ConstraintTrees.increase_variable_indexes-Tuple{Any, Int64}","page":"Reference","text":"increase_variable_indexes(x, incr::Int64) -> Any\n\n\nOffset all variable indexes in a structure x by the given increment.\n\nInternally, this uses renumber_variables.\n\nExtensibility note\n\nIf you extend the functionality of ConstraintTrees by overloading increase_variable_indexes, consider instead providing the overload for renumber_variables which grants more functionality, mainly variable pruning.\n\n\n\n\n\n","title":"ConstraintTrees.increase_variable_indexes"},{"category":"method","location":"reference/#ConstraintTrees.prune_variables-Tuple{Any}","page":"Reference","text":"prune_variables(x) -> Any\n\n\nPrune the unused variable indexes from an object x (such as a ConstraintTree).\n\nThis first runs collect_variables! to determine the actual used variables, then calls renumber_variables to create a renumbered object.\n\n\n\n\n\n","title":"ConstraintTrees.prune_variables"},{"category":"method","location":"reference/#ConstraintTrees.renumber_variables-Tuple{Function, Any}","page":"Reference","text":"renumber_variables(mapping::Function, x) -> Any\n\n\nAn overload of renumber_variables that allows mapping to be a single-parameter Function.\n\n\n\n\n\n","title":"ConstraintTrees.renumber_variables"},{"category":"method","location":"reference/#ConstraintTrees.renumber_variables-Union{Tuple{T}, Tuple{Tree{T}, Any}} where T","page":"Reference","text":"renumber_variables(x::Tree{T}, mapping) -> ConstraintTree\n\n\nRenumber all variables in an object (such as ConstraintTree). The new variable indexes are taken from the mapping parameter at the index of the old variable's index.\n\nThe mapping is assumed to be an array-like object (i.e., it must support getindex, which is used to retrieve the new index for each original variable index).\n\nwarning: The variable index mapping must be monotonic!\nrenumber_variables does not run any consistency checks on the result. The mapping must therefore be monotonically increasing, and the zero index must map to itself, otherwise invalid Values will be produced.\n\n\n\n\n\n","title":"ConstraintTrees.renumber_variables"},{"category":"method","location":"reference/#ConstraintTrees.substitute-Tuple{ConstraintTree, AbstractVector}","page":"Reference","text":"substitute(x::ConstraintTree, y::AbstractVector) -> Any\n\n\nSubstitute variable values from y into the constraint tree's constraint's values, getting a tree with modified constraints.\n\nIn a typical application, this can be used together with prune_variables to fix a subset of variables to known values and effectively remove them from the problem.\n\nCf. substitute_values, which creates a tree of \"plain\" values with no constraints.\n\n\n\n\n\n","title":"ConstraintTrees.substitute"},{"category":"method","location":"reference/#ConstraintTrees.substitute_values-Union{Tuple{T}, Tuple{Tree, AbstractVector}, Tuple{Tree, AbstractVector, Type{T}}} where T","page":"Reference","text":"substitute_values(x::Tree, y::AbstractVector) -> Any\nsubstitute_values(\n    x::Tree,\n    y::AbstractVector,\n    ::Type{T}\n) -> Any\n\n\nSubstitute variable values from y into the constraint tree's constraint's values, getting a tree of \"solved\" constraint values for the given variable assignment.\n\nThe third argument forces the output type (it is forwarded to map). The type gets defaulted from eltype(y).\n\nTo preserve the constraints in the tree, use substitute.\n\n\n\n\n\n","title":"ConstraintTrees.substitute_values"},{"category":"function","location":"reference/#ConstraintTrees.var_count","page":"Reference","text":"Old name for variable_count.\n\nDeprecation warning: This will be removed in a future release.\n\n\n\n\n\n","title":"ConstraintTrees.var_count"},{"category":"method","location":"reference/#ConstraintTrees.variable-Union{Tuple{}, Tuple{T}} where T","page":"Reference","text":"variable(; ...) -> Constraint\nvariable(weight; bound, idx) -> Constraint\n\n\nAllocate a single unnamed variable, returning a Constraint with an optionally specified bound.\n\n\n\n\n\n","title":"ConstraintTrees.variable"},{"category":"method","location":"reference/#ConstraintTrees.variable_count-Tuple{ConstraintTree}","page":"Reference","text":"variable_count(x::ConstraintTree) -> Any\n\n\nFind the expected count of variables in a ConstraintTree.\n\n\n\n\n\n","title":"ConstraintTrees.variable_count"},{"category":"method","location":"reference/#ConstraintTrees.variable_count-Tuple{Constraint}","page":"Reference","text":"variable_count(x::Constraint) -> Int64\n\n\nFind the expected count of variables in a Constraint.\n\n\n\n\n\n","title":"ConstraintTrees.variable_count"},{"category":"method","location":"reference/#ConstraintTrees.variable_count-Tuple{LinearValueT}","page":"Reference","text":"variable_count(x::LinearValueT) -> Int64\n\n\nFind the expected count of variables in a LinearValue. (This is a O(1) operation, relying on the ordering of the indexes.)\n\n\n\n\n\n","title":"ConstraintTrees.variable_count"},{"category":"method","location":"reference/#ConstraintTrees.variable_count-Tuple{QuadraticValueT}","page":"Reference","text":"variable_count(x::QuadraticValueT) -> Int64\n\n\nFind the expected count of variables in a QuadraticValue. (This is a O(1) operation, relying on the co-lexicographical ordering of indexes.)\n\n\n\n\n\n","title":"ConstraintTrees.variable_count"},{"category":"function","location":"reference/#ConstraintTrees.variables","page":"Reference","text":"variables(; ...)\nvariables(weight; keys, bounds)\n\n\nMake a trivial constraint system that creates variables with indexes in range 1:length(keys) named in order as given by keys.\n\nThe individual bounds should be subtypes of Bound, or nothing (which is the default). The bounds are broadcasted; to pass a single bound for all variables, one can use e.g. bounds = EqualTo(0).\n\n\n\n\n\n","title":"ConstraintTrees.variables"},{"category":"function","location":"reference/#ConstraintTrees.variables_for","page":"Reference","text":"variables_for(makebound, ts::Tree) -> Any\nvariables_for(makebound, ts::Tree, weight) -> Any\n\n\nAllocate a variable for each item in a constraint tree (or any other kind of tree) and return a ConstraintTree with variables bounded by the makebound function, which converts a given tree element's value into a bound for the corresponding variable.\n\n\n\n\n\n","title":"ConstraintTrees.variables_for"},{"category":"function","location":"reference/#ConstraintTrees.variables_ifor","page":"Reference","text":"variables_ifor(makebound, ts::Tree) -> Any\nvariables_ifor(makebound, ts::Tree, weight) -> Any\n\n\nLike variables_for but the makebound function also receives a path to the variable, as with imap.\n\n\n\n\n\n","title":"ConstraintTrees.variables_ifor"},{"category":"type","location":"reference/#ConstraintTrees.SerializationLabel","page":"Reference","text":"struct SerializationLabel{L}\n\nHelper type for doing overloads for serialization and deserialization.\n\n\n\n\n\n","title":"ConstraintTrees.SerializationLabel"},{"category":"method","location":"reference/#ConstraintTrees.deserialize-Tuple{Type{Between}, Vector}","page":"Reference","text":"deserialize(_::Type{Between}, x::Vector) -> BetweenT\n\n\nDeserialize a vector of 2 values into a Between bound.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize"},{"category":"method","location":"reference/#ConstraintTrees.deserialize-Tuple{Type{Constraint}, Dict}","page":"Reference","text":"deserialize(_::Type{Constraint}, x::Dict) -> Constraint\n\n\nReconstruct a Constraint from a dictionary as produced by serialize.\n\nTypes of values and bounds in constraints are specified explicitly in the serialized representation; the mapping is specified by overloads of deserialize_bound and deserialize_value.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize"},{"category":"method","location":"reference/#ConstraintTrees.deserialize-Tuple{Type{EqualTo}, Float64}","page":"Reference","text":"deserialize(_::Type{EqualTo}, x::Float64) -> EqualTo\n\n\nDeserialize a single value into an EqualTo bound.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize"},{"category":"method","location":"reference/#ConstraintTrees.deserialize-Tuple{Type{LinearValue}, Dict}","page":"Reference","text":"deserialize(_::Type{LinearValue}, x::Dict) -> LinearValue\n\n\nDeserialize a dictionary with keys idxs and weights into a LinearValue.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize"},{"category":"method","location":"reference/#ConstraintTrees.deserialize-Tuple{Type{QuadraticValue}, Dict}","page":"Reference","text":"deserialize(\n    _::Type{QuadraticValue},\n    x::Dict\n) -> QuadraticValue\n\n\nDeserialize a dictionary with keys idxs and weights into a LinearValue. idxs must contain vectors of length 2 with the double indices.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize"},{"category":"method","location":"reference/#ConstraintTrees.deserialize-Union{Tuple{T}, Tuple{Type{Tree{T}}, Dict}} where T","page":"Reference","text":"deserialize(_::Type{Tree{T}}, x::Dict) -> Tree\n\n\nReconstruct a Tree from a dictionary \"description\" as produced by serialize.\n\nThe trees are labeled with explicit key \"tree\", which allows the implementation to recognize the subtrees from leaf elements.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize"},{"category":"method","location":"reference/#ConstraintTrees.deserialize_bound-Tuple{Type{ConstraintTrees.SerializationLabel{:between}}, Any}","page":"Reference","text":"deserialize_bound(\n    _::Type{ConstraintTrees.SerializationLabel{:between}},\n    x\n) -> BetweenT\n\n\nDeserialize a Bound into a type described by the label.\n\nThis should be overloaded for custom bound types that are supposed to get serialized.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize_bound"},{"category":"method","location":"reference/#ConstraintTrees.deserialize_value-Tuple{Type{ConstraintTrees.SerializationLabel{:linear}}, Any}","page":"Reference","text":"deserialize_value(\n    _::Type{ConstraintTrees.SerializationLabel{:linear}},\n    x\n) -> LinearValue\n\n\nDeserialize a Value into a type described by the label.\n\nThis should be overloaded for custom value types that are supposed to get serialized.\n\n\n\n\n\n","title":"ConstraintTrees.deserialize_value"},{"category":"method","location":"reference/#ConstraintTrees.serialize-Tuple{Between}","page":"Reference","text":"serialize(x::Between) -> Vector{Float64}\n\n\nSerialize a Between bound into a vector of the two interval endpoints.\n\n\n\n\n\n","title":"ConstraintTrees.serialize"},{"category":"method","location":"reference/#ConstraintTrees.serialize-Tuple{Constraint}","page":"Reference","text":"serialize(\n    x::Constraint\n) -> Union{Dict{String, Any}, Dict{String, String}}\n\n\nConvert a Constraint to basic Julia types.\n\nThe types of values and bounds are named explicitly to allow precise deserialization without guessing; the values are found from overloads of serialize_value_label and serialize_bound_label. If the bound is not present, the corresponding data is completely omitted and no label is added.\n\n\n\n\n\n","title":"ConstraintTrees.serialize"},{"category":"method","location":"reference/#ConstraintTrees.serialize-Tuple{EqualTo}","page":"Reference","text":"serialize(x::EqualTo) -> Float64\n\n\nSerialize an EqualTo bound into a number.\n\n\n\n\n\n","title":"ConstraintTrees.serialize"},{"category":"method","location":"reference/#ConstraintTrees.serialize-Tuple{LinearValue}","page":"Reference","text":"serialize(\n    x::LinearValue\n) -> Union{Dict{String, Vector}, Dict{String, Vector{Int64}}}\n\n\nSerialize a LinearValue into a dictionary with coefficient indices and weights.\n\n\n\n\n\n","title":"ConstraintTrees.serialize"},{"category":"method","location":"reference/#ConstraintTrees.serialize-Tuple{QuadraticValue}","page":"Reference","text":"serialize(\n    x::QuadraticValue\n) -> Union{Dict{String, Vector}, Dict{String, Vector{Vector{Int64}}}}\n\n\nSerialize a QuadraticValue into a dictionary with coefficient index tuples and weights.\n\n\n\n\n\n","title":"ConstraintTrees.serialize"},{"category":"method","location":"reference/#ConstraintTrees.serialize-Tuple{Tree}","page":"Reference","text":"serialize(\n    x::Tree\n) -> Dict{String, V} where V<:(Dict{K, V} where {V, K})\n\n\nConvert a Tree into a \"simple\" representation that only consists of basic Julia types.\n\nIn particular, all trees labeled as dictionaries with a single key tree that indexes a dictionary of all keyed entries.\n\n\n\n\n\n","title":"ConstraintTrees.serialize"},{"category":"method","location":"reference/#ConstraintTrees.serialize_bound_label-Tuple{Type{Between}}","page":"Reference","text":"serialize_bound_label(_::Type{Between}) -> Symbol\n\n\nProduce a type label for serializing the given bound type.\n\nOverloads should correspond to deserialize_bound.\n\n\n\n\n\n","title":"ConstraintTrees.serialize_bound_label"},{"category":"method","location":"reference/#ConstraintTrees.serialize_value_label-Tuple{Type{LinearValue}}","page":"Reference","text":"serialize_value_label(_::Type{LinearValue}) -> Symbol\n\n\nProduce a type label for serializing the given value type.\n\nOverloads should correspond to deserialize_value.\n\n\n\n\n\n","title":"ConstraintTrees.serialize_value_label"},{"category":"method","location":"reference/#ConstraintTrees.default_pretty_var-Tuple{Int64}","page":"Reference","text":"default_pretty_var(i::Int64) -> String\n\n\nInternal helper for prettyprinting variable contributions; default value of format_variable keyword argument in pretty.\n\nIf there should be multiplication operators, the implementation format_variable is supposed to prefix the variable contribution. This should not print anything for the zero \"affine\" variable.\n\n\n\n\n\n","title":"ConstraintTrees.default_pretty_var"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{Any}","page":"Reference","text":"pretty(x; kwargs...) -> Any\n\n\nPretty-print a given object via other overloads of pretty, defaulting the output stream to standard output.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, Any}","page":"Reference","text":"pretty(io::IO, x; kwargs...) -> Union{Nothing, Bool}\n\n\nDefault implementation of pretty defaults to Base.show.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, BetweenT}","page":"Reference","text":"pretty(\n    io::IO,\n    x::BetweenT;\n    in_interval_sign,\n    format_bound_value,\n    kwargs...\n)\n\n\nPretty-print an interval bound into the io.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, Bound}","page":"Reference","text":"pretty(io::IO, x::Bound; default_bound_separator, kwargs...)\n\n\nDefault pretty-printing of a Bound. Overloads that print bounds should expect that they are ran right after printing of the Values, on the same line.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, Constraint}","page":"Reference","text":"pretty(\n    io::IO,\n    x::Constraint;\n    kwargs...\n) -> Union{Nothing, Bool}\n\n\nPretty-print a constraint into the io.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, EqualToT}","page":"Reference","text":"pretty(\n    io::IO,\n    x::EqualToT;\n    equal_to_sign,\n    format_bound_value,\n    kwargs...\n)\n\n\nPretty-print an equality bound into the io.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, LinearValueT}","page":"Reference","text":"pretty(\n    io::IO,\n    x::LinearValueT;\n    format_coefficient,\n    format_variable,\n    plus_sign,\n    zero_value,\n    kwargs...\n)\n\n\nPretty-print a linear value into the io.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, QuadraticValueT}","page":"Reference","text":"pretty(\n    io::IO,\n    x::QuadraticValueT;\n    format_coefficient,\n    format_variable,\n    plus_sign,\n    zero_value,\n    kwargs...\n)\n\n\nPretty-print a quadratic value into the io.\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty-Tuple{IO, Tree}","page":"Reference","text":"pretty(io::IO, x::Tree; kwargs...)\n\n\nPretty-print a nested tree into the io. This is the only overload of pretty that is allowed to break lines.\n\nThe printing assumes a Unicode-capable stdout by default; the formatting can be customized via keyword arguments (see other overloads of pretty and pretty_tree).\n\n\n\n\n\n","title":"ConstraintTrees.pretty"},{"category":"method","location":"reference/#ConstraintTrees.pretty_tree-Tuple{IO, Any, Tuple, String, String}","page":"Reference","text":"pretty_tree(\n    io::IO,\n    x,\n    ctxt0::Tuple,\n    pfx0::String,\n    pfx::String;\n    leaf_separator,\n    kwargs...\n)\n\n\nOverload of pretty_tree for anything except Trees – this utilizes pretty to finish a line with the \"contents\" of the tree leaf.\n\n\n\n\n\n","title":"ConstraintTrees.pretty_tree"},{"category":"method","location":"reference/#ConstraintTrees.pretty_tree-Tuple{IO, Tree, Tuple, String, String}","page":"Reference","text":"pretty_tree(\n    io::IO,\n    x::Tree,\n    ctxt0::Tuple,\n    pfx0::String,\n    pfx::String;\n    format_label,\n    singleton_branch,\n    first_branch,\n    middle_branch,\n    last_branch,\n    child_first_indent,\n    child_indent,\n    lastchild_first_indent,\n    lastchild_indent,\n    kwargs...\n)\n\n\nInternal helper for recursive prettyprinting of tree structures. Adds a relatively legible Unicode scaffolding to highlight the tree structure. The scaffolding can be customized via keyword arguments (which are passed here from pretty).\n\nSpecifically, format_label argument can be used convert a tuple with the \"path\" in the tree (as with e.g. imap) into a suitable tree branch label.\n\n\n\n\n\n","title":"ConstraintTrees.pretty_tree"},{"category":"section","location":"#ConstraintTrees.jl","page":"README","text":"","title":"ConstraintTrees.jl"},{"category":"module","location":"#ConstraintTrees.ConstraintTrees","page":"README","text":"Package ConstraintTrees.jl provides a simple data structure ConstraintTree for organizing the contents of linear and quadratic constrained optimization problems. As a main goal, it abstracts over the distinction between constraints and variables, allowing much tidier representation for many kinds of complex constraint systems.\n\nThe primary purpose of ConstraintTrees.jl is to work with COBREXA.jl; but the package is otherwise completely independent, lightweight, dependency-free and usecase-agnostic. Generally, it is intended to be used with JuMP and the documentation uses JuMP for demonstrations, but any other solver framework will do just as well.\n\nThe package is structured as follows:\n\nThere is no representation for variables in the model; instead, values depend on anonymous numbered variables that are mostly hidden in normal use.  This assumption erases the distinction between a \"simple\" variable and a complex derived linear combination, allowing more freedom in model construction. If required, named values may still \"implicitly\" serve as representations for variables.\nVariables may be combined into LinearValues and QuadraticValues, which are affine combinations and quadratic-affine combinations (respectively) of values of some selected variables.\nValues may be bounded to an interval or exact value using a Constraint\nA collection of named Constraints is called a ConstraintTree; it behaves mostly as a specialized Symbol-keyed dictionary.\nConstraintTrees can be very easily organized into subdirectories, combined and made independent on each other using operators ^, *, and + – this forms the basis of the \"tidy\" algebra of constraints.\nA variable assignment, which is typically the \"solution\" for a given constraint tree, can be combined with a ConstraintTree to create a \"value tree\" via substitute_values. Value trees enable browsing of the optimization results in the very same structure as the input ConstraintTree.\n\nYou can follow the examples in documentation and the docstrings of package contents for more details.\n\n\n\n\n\n","title":"ConstraintTrees.ConstraintTrees"},{"category":"section","location":"1-metabolic-modeling/#Example:-Metabolic-modeling","page":"Example: Metabolic modeling","text":"In this example we demonstrate the use of ConstraintTree structure for solving the metabolic modeling tasks. At the same time, we show how to export the structure to JuMP, and use value trees to find useful information about the result.\n\nFirst, let's import some packages:\n\nimport ConstraintTrees as C\n\nWe will need a constraint-based metabolic model; for this test we will use the usual \"E. Coli core metabolism\" model as available from BiGG:\n\nimport Downloads: download\n\ndownload(\"http://bigg.ucsd.edu/static/models/e_coli_core.xml\", \"e_coli_core.xml\")\n\nimport SBML\necoli = SBML.readSBML(\"e_coli_core.xml\")","title":"Example: Metabolic modeling"},{"category":"section","location":"1-metabolic-modeling/#Allocating-and-constraining-variables","page":"Example: Metabolic modeling","text":"Let's first build the constrained representation of the problem. First, we will need a variable for each of the reactions in the model.\n\nc = C.variables(keys = Symbol.(keys(ecoli.reactions)))\n\ninfo: Pretty-printing\nBy default, Julia shows relatively long namespace prefixes before all identifiers, which substantially clutters the output. To improve the pretty-printing, all type names are marked as exported from the package, and you can import them via using ConstraintTrees.\n\nThe above operation returns a ConstraintTree. You can browse these as a dictionary:\n\nc[:R_PFK]\n\n...or much more conveniently using the record dot syntax as properties:\n\nc.R_PFK\n\nThe individual LinearValues in constraints behave like sparse vectors that refer to variables: The first field represents the referenced variable indexes, and the second field represents the coefficients. Compared to the sparse vectors, information about the total number of variables is not stored explicitly.\n\nOperator ^ is used to name individual constraints and directories in the hierarchy. Let us name our constraints as \"fluxes\" (which is a common name in metabolic modeling) and explore the result:\n\nc = :fluxes^c\n\nWe can see that there is now only a single \"top-level directory\" in the constraint system, which can be explored with the dot access again:\n\nc.fluxes.R_PFK\n\nIndexing via values is again possible via the usual bracket notation, and can be freely combined with the dot notation:\n\nc[:fluxes][:R_PFK]","title":"Allocating and constraining variables"},{"category":"section","location":"1-metabolic-modeling/#Adding-single-variable-constraints","page":"Example: Metabolic modeling","text":"Each element in the constraint tree consists of a linear combination of the variables, which can be freely used to construct (and constraint) new linear combinations of variables. As the simplest use, we can constraint the variables (using Constraints) to their valid bounds as defined by the model:\n\nrxn_constraints =\n    let rxn_bounds = Symbol.(keys(ecoli.reactions)) .=> zip(SBML.flux_bounds(ecoli)...)\n        C.ConstraintTree(\n            r => C.Constraint(value = c.fluxes[r].value, bound = (lb, ub)) for\n            (r, ((lb, _), (ub, _))) in rxn_bounds # SBML units are ignored for simplicity\n        )\n    end\n\nNote that in the example we use a simplified Dict-like construction of the ConstraintTree. You might equivalently write the code as a product (using prod()) of constraints that are individually labeled using the ^ operator, but the direct dictionary construction is faster because it skips many intermediate steps, and looks much more like idiomatic Julia code.\n\nTo combine the constraint trees, we can make a nice directory for the constraints and add them to the tree using operator *. Making \"products\" of constraint trees combines the trees in a way that they share their variables. In particular, using the values from c.fluxes in the constraints within rxn_constraints here will constraint precisely the same variables (and thus values) as the ones in the original system.\n\nc = c * :constraints^rxn_constraints\n\nOur model representation now contains 2 \"directories\":\n\ncollect(keys(c))","title":"Adding single-variable constraints"},{"category":"section","location":"1-metabolic-modeling/#Value-and-constraint-arithmetics","page":"Example: Metabolic modeling","text":"Values may be combined additively and multiplied by real constants; which allows us to easily create more complex linear combination of any values already occurring in the model:\n\n3 * c.fluxes.R_PFK.value - c.fluxes.R_ACALD.value / 2\n\nFor simplicity, you can also scale whole constraints, but it is impossible to add them together because the meaning of the bounds would get broken:\n\n(3 * c.fluxes.R_PFK, -c.fluxes.R_ACALD / 2)\n\nTo process constraints in bulk, you may use C.value for easier access to values when making new constraints:\n\nsum(C.value.(values(c.fluxes)))\n\nNotably, ConstraintTrees provide their own implementation of sum which typically works faster when adding many Values together. The basic interface and results are otherwise the same as with the sum from Base:\n\nC.sum(C.value.(values(c.fluxes)))\n\ndanger: `Base.sum` vs. `ConstraintTrees.sum`\nSince the sum from Base package is usually implemented as a left fold, it does not behave optimally when the temporary sub-results grow during the computation (and thus their addition becomes gradually slower). In turn, using the Base.sum for summing up LinearValues and QuadraticValues may take time quadratic in the number of added items. sum from ConstraintTrees uses a different addition order which reduces the amount of large items added together (implemented by \"pairwise\" preduce), and in works in almost-linear time in most cases.","title":"Value and constraint arithmetics"},{"category":"section","location":"1-metabolic-modeling/#Affine-values","page":"Example: Metabolic modeling","text":"To simplify various modeling goals (mainly calculation of various kinds of \"distances\"), the values support inclusion of an affine element – the variable with index 0 is assumed to be the \"affine unit\", and its assigned value is fixed at 1.0.\n\nTo demonstrate, let's make a small system with 2 variables.\n\nsystem = C.variables(keys = [:x, :y])\n\nTo add an affine element to a LinearValue, simply add it as a Real number using +, as in the linear transformations below:\n\nsystem =\n    :original_coords^system *\n    :transformed_coords^C.ConstraintTree(\n        :xt => C.Constraint(1 + system.x.value + 4 + system.y.value),\n        :yt => C.Constraint(0.1 * (3 - system.y.value)),\n    )\n\nC.pretty(system)","title":"Affine values"},{"category":"section","location":"1-metabolic-modeling/#Adding-combined-constraints","page":"Example: Metabolic modeling","text":"Metabolic modeling relies on the fact that the total rates of any metabolite getting created and consumed by the reaction equals to zero (which corresponds to conservation of mass). We can now add corresponding \"stoichiometric\" network constraints by following the reactants and products in the SBML structure:\n\nstoi_constraints = C.ConstraintTree(\n    Symbol(m) => C.Constraint(\n        value = -C.sum(\n            (\n                sr.stoichiometry * c.fluxes[Symbol(rid)].value for\n                (rid, r) in ecoli.reactions for sr in r.reactants if sr.species == m\n            ),\n            init = zero(C.LinearValue), # sometimes the sums are empty\n        ) + C.sum(\n            (\n                sr.stoichiometry * c.fluxes[Symbol(rid)].value for\n                (rid, r) in ecoli.reactions for sr in r.products if sr.species == m\n            ),\n            init = zero(C.LinearValue),\n        ),\n        bound = 0.0,\n    ) for m in keys(ecoli.species)\n);\nnothing #hide\n\nLet's have a closer look at one of the constraints:\n\nstoi_constraints.M_acald_c\n\nAgain, we can label the stoichiometry properly and add it to the bigger model representation:\n\nc = c * :stoichiometry^stoi_constraints","title":"Adding combined constraints"},{"category":"section","location":"1-metabolic-modeling/#Saving-the-objective","page":"Example: Metabolic modeling","text":"Constraint based models typically optimize a certain linear formula. Constraint trees do not support setting objectives (they are not constraints), but we can save the objective as a harmless unconstrained \"constraint\" that can be used later to refer to the objective more easily. We can save that information into the constraint system immediately:\n\nc *=\n    :objective^C.Constraint(\n        C.sum(\n            c.fluxes[Symbol(rid)].value * coeff for\n            (rid, coeff) in (keys(ecoli.reactions) .=> SBML.flux_objective(ecoli)) if\n            coeff != 0.0;\n            init = 0.0,\n        ),\n    )","title":"Saving the objective"},{"category":"section","location":"1-metabolic-modeling/#Constrained-system-solutions-and-value-trees","page":"Example: Metabolic modeling","text":"To aid exploration of variable assignments in the constraint trees, we can convert them to value trees. These have the very same structure as constraint trees, but carry only the \"solved\" constraint values instead of full constraints.\n\nLet's demonstrate this quickly on the example of system with affine variables from above. First, let's assume that someone solved the system (in some way) and produced a solution of variables as follows:\n\nsolution = [1.0, 5.0] # corresponds to :x and :y in order given in `variables`\n\nA value tree for this solution is constructed in a straightforward manner:\n\nst = C.substitute_values(system, solution)\n\nWe can now check the values of the \"original\" coordinates:\n\nst.original_coords\n\nThe more complex constraints get their values computed automatically from the variable assignment:\n\nst.transformed_coords","title":"Constrained system solutions and value trees"},{"category":"section","location":"1-metabolic-modeling/#Solving-the-constraint-system-using-JuMP","page":"Example: Metabolic modeling","text":"We can make a small function that throws our model into JuMP, optimizes it, and gives us back a variable assignment vector. This vector can then be used to determine and browse the values of constraints and variables using a Float64-valued tree.\n\nimport JuMP\nfunction optimized_vars(cs::C.ConstraintTree, objective::C.LinearValue, optimizer)\n    model = JuMP.Model(optimizer)\n    JuMP.@variable(model, x[1:C.variable_count(cs)])\n    JuMP.@objective(model, JuMP.MAX_SENSE, C.substitute(objective, x))\n    C.traverse(cs) do c\n        b = c.bound\n        if b isa C.EqualTo\n            JuMP.@constraint(model, C.substitute(c.value, x) == b.equal_to)\n        elseif b isa C.Between\n            val = C.substitute(c.value, x)\n            isinf(b.lower) || JuMP.@constraint(model, val >= b.lower)\n            isinf(b.upper) || JuMP.@constraint(model, val <= b.upper)\n        end\n    end\n    JuMP.optimize!(model)\n    JuMP.value.(model[:x])\nend\n\nWith this in hand, we can use an external linear problem solver to find the optimum of the constrained system:\n\nimport GLPK\noptimal_variable_assignment = optimized_vars(c, c.objective.value, GLPK.Optimizer)\n\nTo explore the solution more easily, we can make a tree with values that correspond to ones in our constraint tree:\n\nresult = C.substitute_values(c, optimal_variable_assignment)\n\nresult.fluxes.R_BIOMASS_Ecoli_core_w_GAM\n\nresult.fluxes.R_PFK\n\nSometimes it is unnecessary to recover the values for all constraints, so we are better off selecting just the right subtree:\n\nC.substitute_values(c.fluxes, optimal_variable_assignment)\n\nC.substitute_values(c.objective, optimal_variable_assignment)\n\nWe'll save the result for future use at the end of this example:\n\nresult_single_organism = result","title":"Solving the constraint system using JuMP"},{"category":"section","location":"1-metabolic-modeling/#Combining-and-extending-constraint-systems","page":"Example: Metabolic modeling","text":"Constraint trees can be extended with new variables from another constraint trees using the + operator. Contrary to the * operator, adding the constraint trees does not share the variables between operands, and the resulting constraint tree will basically contain two disconnected trees that solve independently. The user is expected to create additional constraints to connect the independent parts.\n\nHere, we demonstrate this by creating a community of two slightly different E. Coli species: First, we disable functionality of a different reaction in each of the models to create a diverse group of differently handicapped organisms:\n\nc =\n    :community^(\n        :species1^(c * :handicap^C.Constraint(c.fluxes.R_PFK.value, 0)) +\n        :species2^(c * :handicap^C.Constraint(c.fluxes.R_ACALD.value, 0))\n    )\n\nWe can create additional variables that represent total community intake of oxygen, and total community production of biomass:\n\nc += :exchanges^C.variables(keys = [:oxygen, :biomass], bounds = [(-10.0, 10.0), nothing])\n\nThese can be constrained so that the total influx (or outflux) of each of the registered metabolites is in fact equal to total consumption or production by each of the species:\n\nc *=\n    :exchange_constraints^C.ConstraintTree(\n        :oxygen => C.Constraint(\n            value = c.exchanges.oxygen.value - c.community.species1.fluxes.R_EX_o2_e.value -\n                    c.community.species2.fluxes.R_EX_o2_e.value,\n            bound = 0.0,\n        ),\n        :biomass => C.Constraint(\n            value = c.exchanges.biomass.value -\n                    c.community.species1.fluxes.R_BIOMASS_Ecoli_core_w_GAM.value -\n                    c.community.species2.fluxes.R_BIOMASS_Ecoli_core_w_GAM.value,\n            bound = 0.0,\n        ),\n    )\n\nLet's see how much biomass are the two species capable of producing together:\n\nresult =\n    C.substitute_values(c, optimized_vars(c, c.exchanges.biomass.value, GLPK.Optimizer))\nresult.exchanges\n\nFinally, we can iterate over all species in the small community and see how much biomass was actually contributed by each:\n\nDict(k => v.fluxes.R_BIOMASS_Ecoli_core_w_GAM for (k, v) in result.community)","title":"Combining and extending constraint systems"},{"category":"section","location":"1-metabolic-modeling/#Modifying-constraint-systems-in-place","page":"Example: Metabolic modeling","text":"Constraint trees can be modified in-place in a way that allows you to easily change small values in the trees without reconstructing them from the ground up.\n\nAlthough in-place modification is extremely convenient and looks much easier than rebuilding the tree, it may be very detrimental to the robustness and efficiency of the programs, for several reasons:\n\nchanging any data breaks assumptions on anything that was already derived from the data\nfor efficiency, the tree structures are not copied by default if there's no need to do it, and only shared by references; which means that a naive change at a single place of the tree may easily change values also in other parts of any trees, including completely different trees\nthe \"convenient way\" of making sure that the above problem never happens is to copy-on-write the whole tree structure, which is typically quite detrimental to memory use and program efficiency\n\ndanger: Rules of thumb for safe use of in-place modification\nOnly use the in-place modifications if:there is code that explicitly makes sure there is no false sharing via references, e.g. using a deep copy\nthe in-place modifications are the last thing happening to the constraint tree before it is used by the solver\nthe in-place modification code is not a part of a re-usable library\nyou are using a suitable wrapper interface such as Accessors.jl\n\nNow, if you are completely sure that ignoring the robustness guidelines will help your code, you can do the in-place tree modifications quite easily using both dot-access and array-index syntax.\n\nYou can thus, e.g., set a single bound:\n\nc.exchanges.oxygen.bound = C.Between(-20.0, 20.0)\n\n...or rebuild a whole constraint (using a tuple shortcut for Between):\n\nc.exchanges.biomass = C.Constraint(c.exchanges.biomass.value, (-20, 20))\n\n...or even add new constraints, here using the index syntax for demonstration:\n\nc[:exchanges][:production_is_zero] = C.Constraint(c.exchanges.biomass.value, 0)\n\n...or remove some constraints (this erases the constraint that was added just above):\n\ndelete!(c.exchanges, :production_is_zero)\n\nIn the end, the flux optimization yields an expectably different result:\n\nresult_with_more_oxygen =\n    C.substitute_values(c, optimized_vars(c, c.exchanges.biomass.value, GLPK.Optimizer))\nresult.exchanges","title":"Modifying constraint systems in-place"},{"category":"section","location":"1-metabolic-modeling/#Alternative:-Using-Accessors.jl","page":"Example: Metabolic modeling","text":"Accessors.jl implement a \"lensy\" way to update immutable data structures. That comes with a nice outcome of doing the right amount of shallow copies for you automatically, thus avoiding much of the technical danger of in-place modifications. (You still lose the equational reasoning on your code, but that may not be an issue at all in usual codebases.)\n\nAccessors interface is used simply through macros @set (which sets a deeply nested field in a structure, returning a modified copy), or with @reset which automatically \"assigns\" the result back to the original variable:\n\nusing Accessors\n\nc = @set c.exchanges.biomass.bound = C.Between(-50, 50)\n\nThe above code is equivalent to:\n\n@reset c.exchanges.biomass.bound = C.Between(-50, 50)\n\n...and it is also possible to use string and symbol indexes to pick the individual tree items:\n\n@reset c[:exchanges][\"biomass\"].bound = C.Between(-50, 50)\n\nAll of these operations give us:\n\nc.exchanges.biomass","title":"Alternative: Using Accessors.jl"},{"category":"section","location":"1-metabolic-modeling/#Seeing-the-differences-between-the-results","page":"Example: Metabolic modeling","text":"ConstraintTrees.jl defines its own version of zip function that can apply a function to the contents of several trees, \"zipping\" them over the same keys in the structure. This is vaguely similar but otherwise not related to the zip from Julia base (similarly, ConstraintTrees.jl have their own specific map).\n\nIn practice, this allows you to create combined trees with various nice properties very quickly. For example, you can find how much the values have changed between our two communities:\n\nC.zip((x, y) -> y - x, result, result_with_more_oxygen, Float64)\n\nThe result is again a Tree, with the contained type specified by the last argument (Float64 in this case). We can explore it right away as the other result trees. Also, it is possible to call this kind of function using the Julia do notation, making the syntax a bit neater:\n\ndifference = C.zip(result, result_with_more_oxygen, Float64) do x, y\n    y - x\nend\n\nExploring the difference works as expected:\n\ndifference.community.species1.fluxes\n\nFor convenience in special cases, zip is also overloaded for 3 arguments. We can, for a completely artificial example, check if the absolute flux change was bigger in the first or in the second organism in the community when compared to the original single-organism flux (which we luckily saved above):\n\nchanges = C.zip(\n    result.community.species1,\n    result.community.species2,\n    result_single_organism,\n    Bool,\n) do s1, s2, orig\n    abs(s1 - orig) > abs(s2 - orig)\nend\n\nchanges.fluxes\n\nMore high-level functions like zip are described in an example on functional tree processing.","title":"Seeing the differences between the results"},{"category":"section","location":"1-metabolic-modeling/#Simplifying-the-modified-constraint-systems","page":"Example: Metabolic modeling","text":"We may notice that some constraints that we placed on the community make solving of some variables quite trivial. For example, we knocked out PFK in species1:\n\nc.community.species1.handicap\n\nObviously, the variable at index 49 must be equal to zero in the whole model, and we could completely remove it from the model. Unfortunately, just removing the constraint will not help: The variable is also referenced from elsewhere (e.g., from stoichiometry) and removing the constraints actually creates additional feasible solutions for the model, leaving us with possibly invalid solutions!\n\nInstead of that, we can substitute a literal zero value into the variable in the whole model, and then use a pruning function to get rid of the (now unused) variable index.\n\nFirst, let's create a list of all variables in the model that we can use for substitution:\n\nvars = [C.variable(; idx).value for idx = 1:C.variable_count(c)]\n\nNow we use a bit of the knowledge about the model structure – the handicaps constraint single variables, so we can substitute for them directly. (If the handicaps constrained larger linear combinations of variables, we would have to resort to algebra.)\n\nvars[c.community.species1.handicap.value.idxs[1]] = zero(C.LinearValue)\nvars[c.community.species2.handicap.value.idxs[1]] = zero(C.LinearValue)\n\nsubstitute can be used to feed these new variables into all variables in the existing constraint system, which we immediately follow by pruning of the variables (pruning is described closer in the functional tree processing example).\n\nc_simplified = C.prune_variables(C.substitute(c, vars))\n\nThe result contains exactly 2 variables less than the original community:\n\n(C.variable_count(c), C.variable_count(c_simplified))\n\nThe constraints that were substituted for are, at this point, roughly equivalent to saying 0 == 0, and most solvers will simply drop them as tautologies:\n\nc_simplified.community.species1.handicap\n\nFinally, the variables are properly substituted for in the other equations, including the stoichiometry: We can compare e.g. the original balance of metabolite f6p (which is consumed by PFK):\n\nc.community.species1.stoichiometry.M_f6p_c\n\n...to the simplified stoichiometry, which no longer refers to the PFK reaction:\n\nc_simplified.community.species1.stoichiometry.M_f6p_c\n\n\n\nThis page was generated using Literate.jl.","title":"Simplifying the modified constraint systems"}]
}
